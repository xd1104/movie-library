# 好雷嗎?（hao-lei-ma）— 專案備忘

> 接手的人／AI 的第一份說明書。開工前讀完；收工後把新的決策寫回來。
> 使用說明（怎麼跑、怎麼申請金鑰、怎麼部署）在 `README.md`，這裡只寫**為什麼**。

## 這是什麼

Benson 看 Netflix 或去電影院之前，用來**一頁看完一部片值不值得看**的網頁 App。
兩個情境分成兩個分頁：**電影院**（週末要不要買票出門）、**串流**（今晚沙發上看什麼）。

**定位：只查評價，不導購。** 老闆 v2 明確要求——不要任何「去訂票」「去 Netflix 看」的按鈕或外連。
平台資訊留著而且位階高（那是「我今晚打得開嗎」的判斷依據），但**只以不可點的資訊標籤呈現**。

## 技術棧（⚠️ 別跟 accounting-book 搞混）

**原生 HTML + CSS + JavaScript，零建置、零執行期依賴。** 資料存 localStorage。PWA。GitHub Pages 靜態部署。

- ❌ 不要引入 React / Vite / TypeScript / 任何 npm 套件。
- Benson 其他 web app（旅途手帳、食譜本、熱量）都是這個路線，這個專案跟它們一致。
  **`accounting-book` 用 React+Vite 是那個專案的特例，不要抄過來。**
- 唯一的 Node 腳本 `scripts/gen-icons.mjs` 是**一次性**的 icon 產生器（純 Node、零依賴），
  產出的 PNG 已經 commit，平常根本不用跑。它不是建置步驟。

## 架構

```
index.html    三個 section：#view-home / #view-detail / #view-setup（同一頁切換，不是多頁）
css/app.css   全部樣式。深色 token 照 lab-ux 定案的 demo
js/config.js  版本號 HLM_VER、API 端點、快取 TTL、平台字典     ← sw.js 也 importScripts 這支
js/store.js   localStorage：偏好 / 金鑰 / 快取（TTL + 總量上限 + 淘汰）
js/api.js     TMDB / OMDb 呼叫、錯誤分類、資料正規化、快取包裝
js/ui.js      畫面片段產生器（純字串，不碰狀態、不碰 DOM）
js/app.js     狀態、路由（hash + pushState）、事件、啟動自我體檢
sw.js         Service Worker
test/         測試（見下一節）；package.json 只有測試用的開發相依
```

**資料流（順序不能改）**：中文片名 → TMDB 拿 `tmdb_id` 與 `imdb_id` → 用 `imdb_id` 打 OMDb 拿三個分數。

## 測試（改任何東西之前先跑一次）

```bash
npm install                    # 只有一個開發相依：jsdom
npm test                       # 跑完全部，印總斷言數
npm run test:mutate -- --dry   # 幾秒：檢查每條突變的目標字串還套不套得上
npm run test:mutate            # 約 6 分鐘：證明「測試真的在保護那些事」
```

**`package.json` 不違反「零依賴」，不要把它當成違規刪掉。**
那條規則講的是**執行期**零依賴——上線的 App 仍然是純靜態、瀏覽器不會下載任何函式庫，
GitHub Pages 也只服務 `index.html` / `css` / `js` / `icons` 那幾個檔。
`jsdom` 只在開發機上跑測試時用得到，`node_modules/` 有進 `.gitignore`。

- `test/harness.mjs`：用 jsdom 把**真的** `index.html` ＋ 5 支真的 JS 跑起來，接一個假的 TMDB／OMDb。
  這裡沒有 mock 掉任何 App 自己的程式碼，斷言的是**真的渲染出來的 DOM**。
- `test/mock-api.mjs`：假 API 與假資料。**永遠只用一看就假的金鑰**（`TMDBKEY_GOOD` 之類），
  `t5` 會掃描 `test/` 底下有沒有 32 碼十六進位字串或 JWT，防止哪天真金鑰被貼進測試檔。
- `test/mutate.mjs`：把每一件「應該被守住的事」各弄壞一次，確認測試真的會紅，跑完用 SHA-256 驗還原。
  **加新功能時一起加一個突變**，否則「有測試」不等於「測試在保護那件事」——
  2026-08-23 那輪 50 個突變裡，我們自己的測試只殺得掉 20 個，就是這樣被退件的。

  ⭐ **這支工具自己的三條鐵律**（2026-08-23 QA 拿探針實測過：壞掉時它會謊報「一切安好」，
  那比沒有工具更危險，因為你會相信它）：
  1. **突變套不上（stale）＝ 失敗，不是警告。** 重構改掉目標字串 → 那條等於什麼都沒驗。
     實例：v1.0.1 只是把版本號 +1，就讓 3 條突變靜默失效。
  2. **基準線本來就紅 → 直接 exit 2。** 基準線紅的話每條突變都會顯示「紅 ✓」，整份結果不可信。
  3. **「預期全綠」豁免名單要斷言長度**（`EXEMPT_COUNT`），擋住有人把礙事的突變改個名字混過去。
     現在是 **2 條**：R1a（層 B 的備援守衛，層 A 會接住）＋ 一個無害對照組。
     要增減豁免，請先想清楚為什麼，再改 `EXEMPT_COUNT`。
     豁免名單裡的突變**變紅了也會被工具擋下**並要求重新檢視——
     實例：R1c 本來在名單裡，補了 P03 測試之後它會紅了，就從名單移出去。
  4. 突變表裡**不可以寫死版本號之類會變的字串**，要從程式碼讀出來（`VERLINE` / `SWBUILD`）。

> 環境限制：這台機器**沒有瀏覽器**（Playwright 下載 chromium 被 proxy 擋），所以
> **版面與真實 API 都沒有被自動測試涵蓋**。CSS 只能做靜態檢查（字級、觸控尺寸、對比度算得出來的部分）。
> 動到版面一定要請 Benson 實機看一眼。

---

## ⭐ 已定案、別誤改（連理由一起寫，別當 bug 修掉）

### 1. 不用任何框架、不做建置
GitHub Pages 直接 serve 原始檔就能跑，改一行 push 就上線，沒有 build 壞掉的可能。
這個 App 的複雜度（三個畫面、兩個 API）不需要框架。**「順手改成 React／加個 Vite」＝退件。**

### 2. 所有路徑一律相對（`./`），不可以用開頭 `/`
GitHub Pages 專案站在 `https://xxx.github.io/hao-lei-ma/` 這種**子路徑**底下。
開頭 `/` 會指到站台根目錄 → 全部 404。manifest 的 `start_url`/`scope`、SW 的 `register(..., {scope:"./"})`
也一樣。**這是踩過的坑，不是潔癖。**

### 3. OMDb 只在詳細頁呼叫，列表頁一行都不准打
OMDb 免費額度**一天 1000 次**，且是**一部片一次呼叫**。列表一頁 20 部＝一次翻頁燒 20 次，
翻幾頁就沒了。所以：
- 列表只顯示 **TMDB 評分**（`now_playing` / `discover` / `search` 一次就連 `vote_average`＋`vote_count` 一起回來，不用多花呼叫）。
- IMDb／爛番茄／Metacritic **只有點進詳細頁才抓**。
- 因此列表的分數必須維持**三層防呆**（見第 5 點），不可以簡化成一個裸數字。

### 4. 詳細頁必須漸進顯示，**不可以改成「等齊再顯示」**
OMDb 不能用中文片名查，一定要先跟 TMDB 要 `imdb_id`，是**兩段串行**；
而且 OMDb 對**台片／新片／冷門片失敗率很高**。等齊才畫，會讓「TMDB 明明有資料」的片整頁卡住甚至永遠空白。
所以：TMDB 的內容先畫（片名／海報／簡介／平台／TMDB 分數），
三個分數格與綜合分數環先放 skeleton，OMDb 到了再補。**這是硬需求，不是視覺偏好。**

附帶規則：**OMDb 回 `Response:"False" / Movie not found` 是常態，不是錯誤**——安靜降級成「查無收錄」，
絕對不可以跳錯誤畫面。只有金鑰錯／額度用完／網路問題才在分數區下面補一行說明。

### 5. 列表分數的三層防呆（照做，別簡化）
1. **形狀不同**：列表是方形膠囊「TMDB│8.2」，詳細頁是圓環 0–100。刻意長得不一樣，避免被讀成綜合分。
2. **標籤寫死在數字旁**＋10 分制一位小數（`8.2`），跟綜合分的整數（`85`）一眼可分。
3. **列表頂端一行說明**講清楚「列表分數是 TMDB 觀眾評分」，只講一次、不要每列重複。

外加**評分人數**：`<50 人` 顯示琥珀色「僅 N 人評，還不準」、`0 人` 膠囊灰化「尚無評分」。
**分數 + 樣本數 = 可判斷**；只有分數會讓「8 人評的 6.1」跟「8910 人評的 8.2」看起來同一個等級。

### 6. 金鑰只存使用者裝置的 localStorage，永遠不進 repo
`hlm_key_tmdb` / `hlm_key_omdb`。這是純前端靜態站，**沒有後端可以藏 key**；
寫進程式碼就是公開在 GitHub 上等人盜刷。所以做法是 App 內設定頁讓他自己貼一次。
- repo 裡不可以出現 `.env`、`config.js`（放真 key 的那種）之類的檔案。
- 開發驗證用假 key，只驗「沒有 key／key 錯／key 對」三條路徑的**行為**。

### 7. 第一次使用是主流程，不是邊角
他第一次打開時**沒有金鑰，看到的第一個畫面就是「還不能用」**。所以：
- **開機就自我體檢**（`boot()` 檢查金鑰），不等他搜尋第一次才報錯。
- 引導畫面用人話講：去哪兩個網站、申請哪一種（TMDB **API Key v3 auth**、不要 Read Access Token；OMDb **FREE 1000/day**）。
- **「OMDb 那封啟用信一定要點」要用警告框明講**——這是最多人卡住的地方，他很可能忘記。
- 設定頁的「**測試連線**」分別測 TMDB 與 OMDb、**各自給明確結果**。這是他唯一能自己確認金鑰有沒有生效的方法。
- **只有 TMDB 沒有 OMDb 也要能用**（少三個分數而已），不可以因為缺一把 key 整個 App 停擺。

### 8. 錯誤要分類，不可以全部叫「發生錯誤」
`js/api.js` 的 `human()` 把錯誤分成：`nokey / badkey / quota / offline / timeout / server / notfound`，
每一種給**人話 + 下一步**（去設定 or 重試）。加新的錯誤來源時**一起加進 `human()`**，
不要在別的地方另外寫一套錯誤字串。

### 9. 觀看平台是資訊標籤，不是按鈕
`<span class="pv">`，`cursor:default`、沒有 active／hover、不是 `<button>`、**沒有任何 `<a>`**。
全 App 只有兩個 `<a>`：設定頁裡申請 TMDB／OMDb 金鑰的連結（那是設定流程必要的，不是導購）。
**刻意不加 disabled／灰化樣式**——灰掉會讓人以為壞了；維持正常對比、只是不可點，讀起來就是資訊。

### 10. 底部那顆返回鈕要留著
詳細頁很長（要捲到底看演職員），iPhone 15/16 這種尺寸**頂部返回鈕單手構不到**。
它不是外連、不違反「只查評價」的定位。頂部返回鈕同時保留，兩顆不衝突。

### 11. 排序與片單來源
- **電影院預設「依評價」**（不是依熱門）：他去電影院要花錢＋出門，第一眼要看到「哪部值得」。
  熱門排序等於把行銷聲量排前面，跟他問的問題不同件事。保留「依熱門」當第二視角。
- **串流不給排序切換**：固定依評分。頭部控制項已經有分頁＋平台篩選，再加會太擠。
- **串流片單只收訂閱可看**（`with_watch_monetization_types=flatrate`）：混進租借片會讓「今晚看什麼」
  變成「今晚花多少錢」。要看租借片仍可用搜尋。
- 串流 discover 是用 `sort_by=popularity.desc` 抓一頁回來，**再在前端依評分排序**。
  直接叫 TMDB `vote_average.desc` 會撈出一堆沒人聽過的冷門高分片，不符合「今晚看什麼」。

### 12. 快取 TTL（省額度的必要設計，別隨手調小）
| 資料 | TTL | 為什麼 |
|---|---|---|
| 電影基本資料 `m:{id}` | 30 天 | 片名／簡介／演員幾乎不變 |
| 觀看平台 `pv:{id}` | 1 天 | 會變動，所以詳細頁底部要顯示「更新於」 |
| OMDb 分數 `o:{imdbId}` | 7 天 | 省最寶貴的那 1000 次 |
| 片單／搜尋 | 6 小時 | |
| provider id 對照 | 30 天 | |

快取放 `localStorage`，key 前綴 `hlm_c:`，有**總量上限**（筆數 + 字元數）與**最舊優先淘汰**；
寫入遇到 QuotaExceeded 會先淘汰一半再重試。**別把快取搬進 Service Worker**（見第 13 點）。

### 13. Service Worker：殼 cache-first，**API 一律不進 SW 快取**
API 快取由 localStorage 那層用 TTL 管，SW 再插一手會服務到舊分數、而且沒有 TTL 概念。
`sw.js` 對 `api.themoviedb.org` / `omdbapi.com` **直接 return 不介入**。海報（`image.tmdb.org`）
另開一個有上限的圖片快取。

**版本管理**：`js/config.js` 的 `HLM_VER` 是版本來源，`sw.js` 用 `importScripts("./js/config.js")` 讀它，
快取名稱是 `hlm-shell-v{HLM_VER}` / `hlm-img-v{HLM_VER}`。
**改版要改兩個地方**：`HLM_VER` **和** `sw.js` 第一行的 `/* build x.y.z */`。
（`t10-guards` 會斷言這兩個數字一致，忘了同步會紅。）
📌 **v1.0.0 從來沒有部署到 Pages 過**，沒有人手上有舊快取 → 這一版不用 +1。
**下一次改動開始才要 +1。**
理由：瀏覽器判斷「SW 有沒有更新」是比對 **sw.js 本身的位元組**，只改 importScripts 進來的 config.js
有機會不觸發更新——壞掉的症狀是「老闆永遠拿不到新版，還以為我們沒改」。那行 build 字串就是第二道保險。
SW 有 `skipWaiting()` + activate 清舊快取 + `clients.claim()`；頁面收到 `controllerchange`
**只跳更新提示、不自動 reload**（見第 17 條）。

### 14. 搜尋只在「送出」時查，不做邊打邊查
1. 中文注音組字時邊打邊查會送出一堆半成品，也**燒 API**。
2. 用原生 `<form>` + `type=submit` + `enterkeyhint="search"`，**不要自己接 keydown 判斷 Enter**
   （IME 組字中按 Enter 會誤送，這是 iOS 上真的會發生的問題）。
輸入框有字但還沒送出時，畫面停在原本的片單；按 ✕ 清除才退出搜尋模式。

### 15. 不顯示假的租借／購買價格
定案 demo 上有「約 NT$90／約 NT$390」，但 **TMDB 免費 API 不提供價格**。
與其寫死一個假數字誤導他，不如誠實：租借／購買分組下面加一行「價格各平台不同，TMDB 不提供價格」。
（若之後真的要價格，只能靠人工維護的對照表，會過時——不建議。）

### 16. 錯誤處理一律 `.then(成功).catch(失敗)`，**不要用 `.then(成功, 失敗)`**
兩個參數的寫法裡，**成功處理函式自己丟出的例外沒有人接**。這在這個 App 上的症狀是：
任何渲染期例外都會變成「骨架永遠轉不完、畫面上沒有任何出路」——使用者只能關掉 App。
（QA 2026-08-23 抓到；同一輪也發現 `testOmdb()` 因為這個寫法，
OMDb 回 HTTP 200 + `Response:"False"`（額度用完就是這樣）時，設定頁的測試連線會永遠停在「測試中…」。）
外部來的資料（平台 key、快取內容、TMDB 欄位）**一律當成可能是壞的**，取值前先守衛。

`state.pf` 的保護要講精確（QA 拆過）：
- **層 A（真正在做事的）**：開機時 `state.pf = state.pf.filter(knownBrand)`，把認不得的 key 濾掉。
  **只在記憶體裡濾，不寫回 localStorage**（他的選擇留著，哪天平台回來還在）。
- **層 B（保險，目前觸發不到）**：`pfNames()` 取名字前再守衛一次。因為有層 A，這層現在永遠不會命中。
  留著是為了「以後有人新增別條讀 `HLM_BRAND[k]` 的路徑」時不會又炸一次。
- 突變測試釘住的範圍（`test/mutate.mjs` 的 R1a／R1c／R1d）：
  拆掉**層 A** 會紅（P03 測「他一動篩選，寫回 localStorage 的必須是乾淨的」）；
  只拆**層 B** 不會紅（層 A 會接住，這條列在豁免名單）；兩層都拆一定紅。
  這不是「兩層獨立守衛」，是「一層在守、一層備援」。

### 17. 更新提示：偵測到新版**不自動 reload**
`clients.claim()` 在**第一次安裝**時也會觸發 `controllerchange`。第一次進來看到的就是設定頁，
他正在貼 32 碼金鑰，自動 reload 會把他打的字整段吃掉、而且他不知道發生什麼事。
所以：① 進站先記 `hadController`，第一次安裝直接 return；② 有新版只跳「有新版本了」提示列，由他自己按。
**不要為了「確保拿到新版」改回自動 reload。**

### 18. Service Worker 的存取一律包 try/catch，而且擺在 `boot()` 之後
有些瀏覽器（私密視窗、部分 App 內建瀏覽器）`"serviceWorker" in navigator` 是 **true**、值卻是 **undefined**。
在模組頂層直接 `navigator.serviceWorker.controller` 會丟 `TypeError`，**整支 `js/app.js` 停在 `boot()` 之前**——
症狀比白畫面更糟：index.html 的靜態骨架還在（品牌列、搜尋框、分頁列都看得到），但什麼都不能按，
看起來像活的。所以 `setupSW()` 整段包 try/catch，而且 `boot()` 先跑：
**App 能不能用，永遠優先於 PWA 的加值功能。**
（2026-08-23 QA 抓到，是修 R-2 時自己帶進來的。以後往頂層搬任何東西都要先問：這行會不會 throw？）

### 19. 「重新抓一次這部片」會連 OMDb 快取一起清
按鈕文案已經承諾「重新抓一次」，只清 TMDB 不清分數就是騙人。
這是手動觸發、頻率極低，額度影響可忽略（PM 拍板）。**不要為了省額度改回去。**
清 `o:` 的動作放在 `loadScores()` 裡、用**剛抓回來的** imdb id，**不要**回頭去讀舊的 `m:` 快取拿 id——
舊的 `m:` 可能已經被 `sweep()` 淘汰掉，那樣就永遠清不到分數。

### 20. 顏色與觸控目標的界線
- `--faint`（`#7d8798`）是為了對比度從 `#6b7484` 調亮一階的：它正好用在 11.5px 的 `.hintline` 與 `.votes`，
  也就是「防止把 TMDB 分數誤讀成綜合分」那三層防呆的第 3 層。**最該讀得清楚的字不可以最淡。**
  其餘顏色照定案 demo，不要順手「統一」。
- 除了最近查詢的 ✕（命中區已放大到 38×38），其餘 <44px 的觸控目標是**照老闆已確認的定案 demo**，
  不是漏做，不要自己放大改版面。

### 21. 未上映永遠優先於「上映中」
TMDB 的 `now_playing` 有時候會夾帶還沒上映的片。標成「上映中」是騙人的，
所以 `metaHTML()` 與詳細頁的判斷都是 `m.upcoming` 先判、`inCinema` 要加 `&& !m.upcoming`。
電影院片單另外顯示上映日（≤7 天標「本週新上映」），因為他說電影院片單有時效性。

### 22. 「我訂了哪些平台」(`hlm_mysubs`) 與「上次篩了什麼」(`hlm_pf`) 是兩件事
- `hlm_mysubs`＝事實（我付錢訂了哪些），在設定頁勾。
- `hlm_pf`＝這次片單上的篩選，在片單上臨時改。
- 規則：**從來沒手動篩過**就用 mysubs 當預設；在設定頁改 mysubs 會**立刻同步**到 pf（不然他要回片單再點一次才生效）；
  之後他在片單上臨時改 pf 不會回頭動 mysubs。

### 23. 其它幾個小決定
- **provider id 會變**：開機用 `/watch/providers/movie?watch_region=TW` 依名稱校正一次（1 次呼叫、快取 30 天），
  校正失敗就用 `js/config.js` 裡的預設 id，不影響主流程。所以看到硬寫的 id 不用緊張，它會自我修正。
- **不在字典裡的平台**（LINE TV、Hami Video、KKTV…）用 TMDB 官方 logo 圖顯示，不亂配色。
  字典裡那 8 個（Netflix / Disney+ / Prime / CATCHPLAY+ / friDay / MyVideo / Apple TV / Google TV）
  維持設計定案的品牌色塊。
- **品牌比對用「命中的關鍵字最長」的那個**（`api.js` 的 `brandKeyByName()`），不可以先命中先贏。
  否則 `Apple TV Plus` 會被 `apple` 的 `"apple tv"` 先咬走 —— Apple TV+（訂閱）就變成 Apple TV（租買商店），
  同一部片會出現兩顆都寫「Apple TV」的標籤。**`HLM_BRAND` 的鍵順序不該決定比對結果。**
  `normProvider()` 與 `syncProviderIds()` 共用這一個函式，兩邊才不會對出不一樣的答案。
- **同品牌變體要去重**：TW 的 `flatrate` 可能同時回 `Netflix` 與 `Netflix basic with Ads`，
  兩個都會配到 `netflix` 這個 key。依 key 去重、留 `display_priority` 最小的，
  不然畫面上會出現兩顆一模一樣的標籤。
- **`store.js` 的 `entryTime()` 一定要真的 `JSON.parse`**，不可以用正則撈 `"t":123`。
  正則寫法會被 `JSON.stringify` 的欄位順序牽著走，順序一變**淘汰邏輯會靜默失效**（不報錯，只是永遠砍錯東西）。

### 24. localStorage 全部包 try/catch
無痕模式與部分 in-app 瀏覽器碰 localStorage 會直接丟例外。失敗時退回**記憶體**，
**不可以讓 App 白畫面**；設定頁會明講「這個瀏覽器不讓我存資料」。

---

## 待辦 / 已知粗糙處

- **沒有用真金鑰跑過真實 API**（開發環境拿不到、也不該拿）。所有驗證都是用假的 TMDB／OMDb 回應跑的，
  驗的是**行為與錯誤處理**，不是真實資料長相。第一次用真 key 時要重點看：
  TMDB 台灣 `now_playing` 回幾部、`discover` 的台灣訂閱片單品質、OMDb 對台片的失敗率。
- **沒有在真的 iPhone Safari 上跑過**（開發環境沒有瀏覽器可用）。版面 CSS 與定案 demo 幾乎逐條相同，
  但 PWA 加到主畫面、safe-area、iOS 側滑返回這幾件事**要 Benson 實機確認**。
- 串流／搜尋列表的平台色塊是背景逐片抓 `watch/providers`（最多 20 次 TMDB 呼叫、24 小時快取）。
  TMDB 沒有每日額度所以可行，但**如果之後覺得慢或吵，第一個該砍的就是這段**（`app.js` 的 `fillProviders`）。
- 片單固定只抓第 1 頁（20 部），沒有做分頁／無限捲動。
- 沒有「看過了／不想看」標記，串流片單反覆看到同幾部的機率高。
- **租借／購買沒有價格**（TMDB 不給），只列得到哪裡有。
- 2026-08-23 QA 用 50 個突變壓測過，測試套件目前守得住的清單見 scratchpad 的 `mutverify.mjs`；
  仍有一批「弄壞了測試也不會紅」的地方沒補（例如 SW 攔截 OMDb、`.hide` 樣式失效、
  競態守衛、最近查詢上限、地區/語言常數）。下一輪要動這些地方的人，**先補測試再改**。

## 還沒拍板、要問 Benson 的

1. 列表只有 TMDB 分數（看不到 IMDb／爛番茄），可不可以接受？
   若堅持要，唯一解是「只對前 10 部預抓 OMDb」——額度會吃緊，且捲到後面仍然缺。
2. 綜合分數環的算法目前是**有值分數 ≥2 項才算的純算術平均**（不是 AI 判斷）。要不要加權（例如影評 vs 觀眾）？
3. ~~電影院片單顯示上映日~~ → 2026-08-23 PM 拍板要做，已完成（≤7 天標「本週新上映」）。
4. ~~讓他先勾「我有訂哪些平台」~~ → 2026-08-23 PM 拍板要做，已完成（設定頁，見第 22 條）。

---

## 給 AI 的規則

### 開工前
1. 讀這份 CLAUDE.md ＋ `README.md`，特別是上面「已定案、別誤改」。
2. 有份量的任務（新功能、改架構、跨多檔案）→ 建議 Benson 打 `/team-lab` 走 lab-ux → lab-dev → lab-qa。
   小事自己做完就好，開團隊很花額度。

### 動手時
- 繁體中文註解，跟著既有命名與檔案分工（`ui.js` 只產字串、`api.js` 只管資料、`app.js` 才碰 DOM 與狀態）。
- 動 UI 前先看有沒有違反「已定案」那段。
- **改任何行為都要自己實測**，不要只靠讀程式碼推論。

### 收工前的檢查清單
1. `npm test` 全綠。
2. `npm run test:mutate -- --dry` —— **重構過就一定要跑**，確認沒有突變被改到失效。
3. 有動到行為 → 補測試 ＋ 補一條對應的突變，並用 `--only=` 證明它會紅。
4. 有動到使用者看得到的東西 → `HLM_VER` 與 `sw.js` 的 `/* build */` **兩個都 +1**。

### 收工後（強制）
- 新的決策、踩到的雷、新的「別誤改」界線 → **寫回這份 CLAUDE.md，連理由一起寫**。
- 跨專案通用的教訓 → 回報時明講「這條該進手冊」。
- `HLM_VER` 有沒有 +1？

### 溝通
- 結論先給短的；Benson 是老闆、不看技術細節。
- 不確定就說不確定。測不到就說測不到，不要宣稱驗過沒驗的東西。
