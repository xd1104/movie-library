# 好雷嗎?（hao-lei-ma）— 專案備忘

> 接手的人／AI 的第一份說明書。開工前讀完；收工後把新的決策寫回來。
> 使用說明（怎麼跑、怎麼申請金鑰、怎麼部署）在 `README.md`，這裡只寫**為什麼**。

## 這是什麼

Benson 看 Netflix 或去電影院之前，用來**一頁看完一部片值不值得看**的網頁 App。
兩個情境分成兩個分頁：**電影院**（週末要不要買票出門）、**串流**（今晚沙發上看什麼）。

**定位：只查評價，不導購。** 老闆 v2 明確要求——不要任何「去訂票」「去 Netflix 看」的按鈕或外連。
平台資訊留著而且位階高（那是「我今晚打得開嗎」的判斷依據），但**只以不可點的資訊標籤呈現**。

## 技術棧（⚠️ 別跟 accounting-book 搞混）

**原生 HTML + CSS + JavaScript，零建置、零執行期依賴。** 資料存 localStorage。PWA。GitHub Pages 靜態部署。

- ❌ 不要引入 React / Vite / TypeScript / 任何 npm 套件。
- Benson 其他 web app（旅途手帳、食譜本、熱量）都是這個路線，這個專案跟它們一致。
  **`accounting-book` 用 React+Vite 是那個專案的特例，不要抄過來。**
- 唯一的 Node 腳本 `scripts/gen-icons.mjs` 是**一次性**的 icon 產生器（純 Node、零依賴），
  產出的 PNG 已經 commit，平常根本不用跑。它不是建置步驟。

## 架構

```
index.html    三個 section：#view-home / #view-detail / #view-setup（同一頁切換，不是多頁）
              ＋ body 最前面的 #splash（開場畫面），head 裡有開場的落地色票與 SPLASH_CONFIG
css/app.css   配色與版面。深色 token 照 lab-ux 定案的 demo
css/motion.css 動效 token 與規則（**只有動效、一個顏色都沒有**，見第 30 條）
css/splash.css 開場畫面（印記風格）的長相，範本複製品，落地值寫在 index.html
js/splash.js  開場流程＋從鑰匙圈讀外觀，範本複製品（不可以在這裡改，見第 30 條）
js/config.js  版本號 HLM_VER、API 端點、快取 TTL、平台字典     ← sw.js 也 importScripts 這支
js/store.js   localStorage：偏好 / 金鑰 / 快取（TTL + 總量上限 + 淘汰）
js/api.js     TMDB / OMDb 呼叫、錯誤分類、資料正規化、快取包裝
js/ui.js      畫面片段產生器（純字串，不碰狀態、不碰 DOM）
js/app.js     狀態、路由（hash + pushState）、事件、啟動自我體檢
sw.js         Service Worker
test/         測試（見下一節）；package.json 只有測試用的開發相依
```

**資料流（順序不能改）**：中文片名 → TMDB 拿 `tmdb_id` 與 `imdb_id` → 用 `imdb_id` 打 OMDb 拿三個分數。

## 測試（改任何東西之前先跑一次）

```bash
npm install                    # 只有一個開發相依：jsdom
npm test                       # 跑完全部，印總斷言數
npm run test:mutate -- --dry   # 幾秒：檢查每條突變的目標字串還套不套得上
npm run test:mutate            # 約 6 分鐘：證明「測試真的在保護那些事」
```

**`package.json` 不違反「零依賴」，不要把它當成違規刪掉。**
那條規則講的是**執行期**零依賴——上線的 App 仍然是純靜態、瀏覽器不會下載任何函式庫，
GitHub Pages 也只服務 `index.html` / `css` / `js` / `icons` 那幾個檔。
`jsdom` 只在開發機上跑測試時用得到，`node_modules/` 有進 `.gitignore`。

- `test/harness.mjs`：用 jsdom 把**真的** `index.html` ＋ 5 支真的 JS 跑起來，接一個假的 TMDB／OMDb。
  這裡沒有 mock 掉任何 App 自己的程式碼，斷言的是**真的渲染出來的 DOM**。
- `test/mock-api.mjs`：假 API 與假資料。**永遠只用一看就假的金鑰**（`TMDBKEY_GOOD` 之類），
  `t5` 會掃描 `test/` 底下有沒有 32 碼十六進位字串或 JWT，防止哪天真金鑰被貼進測試檔。
- `test/mutate.mjs`：把每一件「應該被守住的事」各弄壞一次，確認測試真的會紅，跑完用 SHA-256 驗還原。
  **加新功能時一起加一個突變**，否則「有測試」不等於「測試在保護那件事」——
  2026-08-23 那輪 50 個突變裡，我們自己的測試只殺得掉 20 個，就是這樣被退件的。

  ⭐ **這支工具自己的三條鐵律**（2026-08-23 QA 拿探針實測過：壞掉時它會謊報「一切安好」，
  那比沒有工具更危險，因為你會相信它）：
  1. **突變套不上（stale）＝ 失敗，不是警告。** 重構改掉目標字串 → 那條等於什麼都沒驗。
     實例：v1.0.1 只是把版本號 +1，就讓 3 條突變靜默失效。
  2. **基準線本來就紅 → 直接 exit 2。** 基準線紅的話每條突變都會顯示「紅 ✓」，整份結果不可信。
  3. **「預期全綠」豁免名單要斷言長度**（`EXEMPT_COUNT`），擋住有人把礙事的突變改個名字混過去。
     現在是 **2 條**：R1a（層 B 的備援守衛，層 A 會接住）＋ 一個無害對照組。
     要增減豁免，請先想清楚為什麼，再改 `EXEMPT_COUNT`。
     豁免名單裡的突變**變紅了也會被工具擋下**並要求重新檢視——
     實例：R1c 本來在名單裡，補了 P03 測試之後它會紅了，就從名單移出去。
  4. 突變表裡**不可以寫死版本號之類會變的字串**，要從程式碼讀出來（`VERLINE` / `SWBUILD`）。

> 環境限制：Playwright 下載 chromium 被 proxy 擋，所以 **`npm test` 裡沒有任何真實版面測量**，
> CSS 只能做靜態檢查（字級、觸控尺寸、對比度算得出來的部分）。動到版面一定要請 Benson 實機看一眼。
>
> ⚠️ **但這台機器有裝一般的 Chrome**（`C:/Program Files/Google/Chrome/Application/chrome.exe`），
> 可以用 `--headless=new --virtual-time-budget --dump-dom` 做一次性的真實量測
> （2026-08-25 就是這樣量到觸控目標、sticky、橫向溢出的實際數字）。三個雷：
> ① **一定要給 `--user-data-dir` 指到暫存資料夾**，否則會去搶 Benson 正在用的那個 profile 而**整個卡住**；
> ② 靜態伺服器要跑在**另一個 process**——`execFileSync` 會把 Node 的事件迴圈鎖住，同一個 process 裡的
> server 根本回不了請求；③ **量動畫不能用 `--dump-dom`，要開 CDP 連線即時取樣**。
>
> ⚠️ 第 ③ 條的原始寫法是「headless 不會真的推進 CSS 動畫」——**那是錯的**，2026-08-26 QA 在同一台機器、
> 同一支 Chrome 上推翻了：改用 `--headless=new --remote-debugging-port` 開一條 CDP 連線、依真實時間取樣，
> 量到 **`document.getAnimations()` 有 11 條 playState=running**、片單進場逐幀取到 **42 個相異的 computed
> transform**（`matrix(0.985,…,0,10)` → `matrix(1,…,0.0002)` → `none`），動畫結束後 `getAnimations().length = 0`
> 且無殘留 transform；還能用 `Input.dispatchMouseEvent` 發真的滑鼠事件實壓 `:active`。
> 量不到的是 `--virtual-time-budget --dump-dom` 那種**一次性快照**，**差別在方法不是環境**。
>
> **為什麼要特別寫清楚**：`fill:both` 把 `:active` 永久蓋掉（按鈕從此按不出回饋）這一類 bug，
> **只有這種即時取樣的量法抓得到**，純功能測試與靜態斷言都抓不到。留著錯誤的敘述，
> 下一個人就會以「headless 量不到」為由跳過這類驗證。

---

## ⭐ 已定案、別誤改（連理由一起寫，別當 bug 修掉）

### 1. 不用任何框架、不做建置
GitHub Pages 直接 serve 原始檔就能跑，改一行 push 就上線，沒有 build 壞掉的可能。
這個 App 的複雜度（三個畫面、兩個 API）不需要框架。**「順手改成 React／加個 Vite」＝退件。**

### 2. 所有路徑一律相對（`./`），不可以用開頭 `/`
GitHub Pages 專案站在 `https://xxx.github.io/hao-lei-ma/` 這種**子路徑**底下。
開頭 `/` 會指到站台根目錄 → 全部 404。manifest 的 `start_url`/`scope`、SW 的 `register(..., {scope:"./"})`
也一樣。**這是踩過的坑，不是潔癖。**

### 3. OMDb 只在詳細頁呼叫，列表頁一行都不准打
OMDb 免費額度**一天 1000 次**，且是**一部片一次呼叫**。列表一頁 20 部＝一次翻頁燒 20 次，
翻幾頁就沒了。所以：
- 列表只顯示 **TMDB 評分**（`now_playing` / `discover` / `search` 一次就連 `vote_average`＋`vote_count` 一起回來，不用多花呼叫）。
- IMDb／爛番茄／Metacritic **只有點進詳細頁才抓**。
- 因此列表的分數必須維持**三層防呆**（見第 5 點），不可以簡化成一個裸數字。

### 4. 詳細頁必須漸進顯示，**不可以改成「等齊再顯示」**
OMDb 不能用中文片名查，一定要先跟 TMDB 要 `imdb_id`，是**兩段串行**；
而且 OMDb 對**台片／新片／冷門片失敗率很高**。等齊才畫，會讓「TMDB 明明有資料」的片整頁卡住甚至永遠空白。
所以：TMDB 的內容先畫（片名／海報／簡介／平台／TMDB 分數），
三個分數格與綜合分數環先放 skeleton，OMDb 到了再補。**這是硬需求，不是視覺偏好。**

附帶規則：**OMDb 回 `Response:"False" / Movie not found` 是常態，不是錯誤**——安靜降級成「查無收錄」，
絕對不可以跳錯誤畫面。只有金鑰錯／額度用完／網路問題才在分數區下面補一行說明。

### 5. 列表分數的三層防呆（照做，別簡化）
1. **形狀不同**：列表是方形膠囊「TMDB│8.2」，詳細頁是圓環 0–100。刻意長得不一樣，避免被讀成綜合分。
2. **標籤寫死在數字旁**＋10 分制一位小數（`8.2`），跟綜合分的整數（`85`）一眼可分。
3. **列表頂端一行說明**講清楚「列表分數是 TMDB 觀眾評分」，只講一次、不要每列重複。

外加**評分人數**：`<50 人` 顯示琥珀色「僅 N 人評，還不準」、`0 人` 膠囊灰化「尚無評分」。
**分數 + 樣本數 = 可判斷**；只有分數會讓「8 人評的 6.1」跟「8910 人評的 8.2」看起來同一個等級。

### 6. 金鑰只存使用者裝置的 localStorage，永遠不進 repo
> ⚠️ **v1.3.0 更新**：平常的金鑰**來自鑰匙圈的公開值**（那是刻意公開的免費查詢金鑰，
> 見第 29 條），不再需要他手貼。這一條講的「不進 repo」仍然成立——
> **這個 repo 裡永遠不會有金鑰**，公開值放在 `xd1104/keyring` 那邊，由老闆自己決定要不要公開。
`hlm_key_tmdb` / `hlm_key_omdb`。這是純前端靜態站，**沒有後端可以藏 key**；
寫進程式碼就是公開在 GitHub 上等人盜刷。所以做法是 App 內設定頁讓他自己貼一次。
- repo 裡不可以出現 `.env`、`config.js`（放真 key 的那種）之類的檔案。
- 開發驗證用假 key，只驗「沒有 key／key 錯／key 對」三條路徑的**行為**。

### 7. 第一次使用是主流程，不是邊角
他第一次打開時**沒有金鑰，看到的第一個畫面就是「還不能用」**。所以：
- **開機就自我體檢**（`boot()` 檢查金鑰），不等他搜尋第一次才報錯。
- 引導畫面用人話講：去哪兩個網站、申請哪一種（TMDB **API Key v3 auth**、不要 Read Access Token；OMDb **FREE 1000/day**）。
- **「OMDb 那封啟用信一定要點」要用警告框明講**——這是最多人卡住的地方，他很可能忘記。
- 設定頁的「**測試連線**」分別測 TMDB 與 OMDb、**各自給明確結果**。這是他唯一能自己確認金鑰有沒有生效的方法。
- **只有 TMDB 沒有 OMDb 也要能用**（少三個分數而已），不可以因為缺一把 key 整個 App 停擺。

### 8. 錯誤要分類，不可以全部叫「發生錯誤」
`js/api.js` 的 `human()` 把錯誤分成：`nokey / badkey / quota / offline / timeout / server / notfound`，
每一種給**人話 + 下一步**（去設定 or 重試）。加新的錯誤來源時**一起加進 `human()`**，
不要在別的地方另外寫一套錯誤字串。

### 9. 觀看平台是資訊標籤，不是按鈕（PTT 文章連結是**唯一的例外**）
`<span class="pv">`，`cursor:default`、沒有 active／hover、不是 `<button>`、**沒有任何 `<a>`**。
全 App 只有兩個 `<a>`：設定頁裡申請 TMDB／OMDb 金鑰的連結（那是設定流程必要的，不是導購）。
**刻意不加 disabled／灰化樣式**——灰掉會讓人以為壞了；維持正常對比、只是不可點，讀起來就是資訊。

> ⚠️ **2026-08-23 更新：全 App 現在有 3 個 `<a>`** —— 兩個申請金鑰的連結，加上 **PTT 文章連結**。
> 界線是「出去之後在做什麼」：**掏錢／播放＝導購，禁止**；**繼續讀評價＝允許**。
> PTT 連結是老闆親自指定的功能（他的原始痛點就是「看片前會去 PTT 看評論」），
> 不在「不導購」禁令裡（規格 §9.7）。**平台區塊仍然是不可點的純標籤，不要順手改成可點。**
> 有人要以鐵律為由拿掉 PTT 連結 → 先問老闆。

### 10. 底部那顆返回鈕要留著
詳細頁很長（要捲到底看演職員），iPhone 15/16 這種尺寸**頂部返回鈕單手構不到**。
它不是外連、不違反「只查評價」的定位。頂部返回鈕同時保留，兩顆不衝突。

### 11. 排序：兩個分頁**都預設「依熱門」**（⚠️ 不要改回依評價）

> **決策來源（2026-08-23，老闆本人）**：他用過之後明確要求
> 「**可以把一些比較熱門的片放在比較上面的地方嗎，電影院跟串流都是**」。
> PM 有提出疑慮並給了兩個選項：A 完全照熱門、B 熱門與評價加權混合，
> 也明講了 A 的缺點——「純照熱門排，那跟直接打開 Netflix 首頁沒兩樣，
> 你原本的痛點是不知道好不好看」。**他在知情的情況下選了 A（完全照熱門）。**
>
> ⚠️ **這不是疏忽，不要改回依評價，也不要自作主張改成加權混合。**
> 想動之前先問他。使用者仍可用切換鈕自己改成依評價。

- **電影院**：預設 `pop`，依 `now_playing` 回傳的 `popularity` 由高到低。
- **串流**：預設 `pop`，`discover` 本來就帶 `sort_by=popularity.desc`
  → **直接用 API 回來的順序，前端不再重排**（TMDB 回的 `popularity` 欄位跟它自己的排序不見得完全一致，
  前端再排一次反而會跟 API 打架）。
- **兩個分頁都有「依熱門／依評價」切換鈕**（搜尋結果沒有，那裡照 TMDB 的相關度排）。
  兩個分頁**共用同一個偏好**，切換鈕在哪一頁按都一樣——一個心智模型就好。
- **`popularity` 缺值或相同時，一律用 `id` 遞增當固定的第二順位**（`byPop()`）。
  沒有這個的話，同一份片單每次重整順序會跳動；這種抖動使用者會覺得「怪怪的」但講不出哪裡怪。
- **偏好存 `hlm_sort2`，不是 `hlm_sort`**。換 key 是刻意的**資料遷移**：
  舊裝置已經存著 `hlm_sort="score"`，只改預設值的話那台會繼續吃舊值、等於沒改。
  換 key 讓所有裝置**取得一次新預設**，之後再尊重使用者的選擇；舊 key 在開機時清掉。
  → **以後要改任何「有存起來的預設值」，都要用這招，不然只有新裝置吃得到。**

### 11b. 片單來源
- **串流片單只收訂閱可看**（`with_watch_monetization_types=flatrate`）：混進租借片會讓「今晚看什麼」
  變成「今晚花多少錢」。要看租借片仍可用搜尋。
- 串流 discover 用 `sort_by=popularity.desc` 抓一頁回來。
  直接叫 TMDB `vote_average.desc` 會撈出一堆沒人聽過的冷門高分片，不符合「今晚看什麼」。

> 📌 這條**推翻了 lab-ux 定案規格的 §3.3**（原本寫「電影院預設依評價」「串流不給排序切換」）。
> 規格是設計階段的判斷，老闆用過之後的要求優先。

### 12. 快取 TTL（省額度的必要設計，別隨手調小）
| 資料 | TTL | 為什麼 |
|---|---|---|
| 電影基本資料 `m:{id}` | 30 天 | 片名／簡介／演員幾乎不變 |
| 觀看平台 `pv:{id}` | 1 天 | 會變動，所以詳細頁底部要顯示「更新於」 |
| OMDb 分數 `o:{imdbId}` | 7 天 | 省最寶貴的那 1000 次 |
| 片單／搜尋 | 6 小時 | |
| provider id 對照 | 30 天 | |
| **PTT 鄉民風向** | **沒有 TTL** | **刻意的**，見下面 |

> ⚠️ **PTT 資料不在這套 TTL 快取裡，也不要幫它加一個。**（2026-08-23 QA 抓到：原本有一個
> `ttl.ptt = 6 小時` 只被拿去算一個沒有人讀的 `fresh` 欄位，等於騙下一個接手的人，已刪。）
> 它是 **network-first**：每個 session 抓一次網路，抓不到才吃 `hlm_ptt` 那份離線副本（**不論多舊**）。
> 「資料新不新」一律看 **JSON 裡的 `updated`**（門檻 `pttStaleDays` 3 天），**不是看什麼時候抓的**——
> 爬蟲掛掉時 fetch 會成功、`updated` 卻是舊的，用抓取時間判斷會完全看不出來。
> 📌 這一條**推翻了 lab-ux 規格 §9.9 裡「TTL 6 小時」那句**（規格自己同時寫了 network-first，兩者衝突）。
> `hlm_ptt` 裡存的 `t` 只是給人除錯用，程式沒有任何地方讀它。
> 它雖然不在 `hlm_c:` 命名空間，但**設定頁的「清掉暫存資料」會一起清掉它**（`cacheClear()`），
> 不然按鈕的語意是騙人的。

快取放 `localStorage`，key 前綴 `hlm_c:`，有**總量上限**（筆數 + 字元數）與**最舊優先淘汰**；
寫入遇到 QuotaExceeded 會先淘汰一半再重試。**別把快取搬進 Service Worker**（見第 13 點）。

### 13. Service Worker：殼 cache-first，**API 一律不進 SW 快取**
API 快取由 localStorage 那層用 TTL 管，SW 再插一手會服務到舊分數、而且沒有 TTL 概念。
`sw.js` 對 `api.themoviedb.org` / `omdbapi.com` **直接 return 不介入**。海報（`image.tmdb.org`）
另開一個有上限的圖片快取。

**版本管理**：`js/config.js` 的 `HLM_VER` 是版本來源，`sw.js` 用 `importScripts("./js/config.js")` 讀它，
快取名稱是 `hlm-shell-v{HLM_VER}` / `hlm-img-v{HLM_VER}`。
**改版要改兩個地方**：`HLM_VER` **和** `sw.js` 第一行的 `/* build x.y.z */`。
（`t10-guards` 會斷言這兩個數字一致，忘了同步會紅。）
📌 **v1.0.0 從來沒有部署到 Pages 過**，沒有人手上有舊快取 → 這一版不用 +1。
**下一次改動開始才要 +1。**
理由：瀏覽器判斷「SW 有沒有更新」是比對 **sw.js 本身的位元組**，只改 importScripts 進來的 config.js
有機會不觸發更新——壞掉的症狀是「老闆永遠拿不到新版，還以為我們沒改」。那行 build 字串就是第二道保險。
SW 有 `skipWaiting()` + activate 清舊快取 + `clients.claim()`；頁面收到 `controllerchange`
**只跳更新提示、不自動 reload**（見第 17 條）。

### 14. 搜尋只在「送出」時查，不做邊打邊查
1. 中文注音組字時邊打邊查會送出一堆半成品，也**燒 API**。
2. 用原生 `<form>` + `type=submit` + `enterkeyhint="search"`，**不要自己接 keydown 判斷 Enter**
   （IME 組字中按 Enter 會誤送，這是 iOS 上真的會發生的問題）。
輸入框有字但還沒送出時，畫面停在原本的片單；按 ✕ 清除才退出搜尋模式。

### 15. 不顯示假的租借／購買價格
定案 demo 上有「約 NT$90／約 NT$390」，但 **TMDB 免費 API 不提供價格**。
與其寫死一個假數字誤導他，不如誠實：租借／購買分組下面加一行「價格各平台不同，TMDB 不提供價格」。
（若之後真的要價格，只能靠人工維護的對照表，會過時——不建議。）

### 16. 錯誤處理一律 `.then(成功).catch(失敗)`，**不要用 `.then(成功, 失敗)`**
兩個參數的寫法裡，**成功處理函式自己丟出的例外沒有人接**。這在這個 App 上的症狀是：
任何渲染期例外都會變成「骨架永遠轉不完、畫面上沒有任何出路」——使用者只能關掉 App。
（QA 2026-08-23 抓到；同一輪也發現 `testOmdb()` 因為這個寫法，
OMDb 回 HTTP 200 + `Response:"False"`（額度用完就是這樣）時，設定頁的測試連線會永遠停在「測試中…」。）
外部來的資料（平台 key、快取內容、TMDB 欄位）**一律當成可能是壞的**，取值前先守衛。

`state.pf` 的保護要講精確（QA 拆過）：
- **層 A（真正在做事的）**：開機時 `state.pf = state.pf.filter(knownBrand)`，把認不得的 key 濾掉。
  **只在記憶體裡濾，不寫回 localStorage**（他的選擇留著，哪天平台回來還在）。
- **層 B（保險，目前觸發不到）**：`pfNames()` 取名字前再守衛一次。因為有層 A，這層現在永遠不會命中。
  留著是為了「以後有人新增別條讀 `HLM_BRAND[k]` 的路徑」時不會又炸一次。
- 突變測試釘住的範圍（`test/mutate.mjs` 的 R1a／R1c／R1d）：
  拆掉**層 A** 會紅（P03 測「他一動篩選，寫回 localStorage 的必須是乾淨的」）；
  只拆**層 B** 不會紅（層 A 會接住，這條列在豁免名單）；兩層都拆一定紅。
  這不是「兩層獨立守衛」，是「一層在守、一層備援」。

### 17. 更新提示：偵測到新版**不自動 reload**
`clients.claim()` 在**第一次安裝**時也會觸發 `controllerchange`。第一次進來看到的就是設定頁，
他正在貼 32 碼金鑰，自動 reload 會把他打的字整段吃掉、而且他不知道發生什麼事。
所以：① 進站先記 `hadController`，第一次安裝直接 return；② 有新版只跳「有新版本了」提示列，由他自己按。
**不要為了「確保拿到新版」改回自動 reload。**

### 18. Service Worker 的存取一律包 try/catch，而且擺在 `boot()` 之後
有些瀏覽器（私密視窗、部分 App 內建瀏覽器）`"serviceWorker" in navigator` 是 **true**、值卻是 **undefined**。
在模組頂層直接 `navigator.serviceWorker.controller` 會丟 `TypeError`，**整支 `js/app.js` 停在 `boot()` 之前**——
症狀比白畫面更糟：index.html 的靜態骨架還在（品牌列、搜尋框、分頁列都看得到），但什麼都不能按，
看起來像活的。所以 `setupSW()` 整段包 try/catch，而且 `boot()` 先跑：
**App 能不能用，永遠優先於 PWA 的加值功能。**
（2026-08-23 QA 抓到，是修 R-2 時自己帶進來的。以後往頂層搬任何東西都要先問：這行會不會 throw？）

### 19. 「重新抓一次這部片」會連 OMDb 快取一起清
按鈕文案已經承諾「重新抓一次」，只清 TMDB 不清分數就是騙人。
這是手動觸發、頻率極低，額度影響可忽略（PM 拍板）。**不要為了省額度改回去。**
清 `o:` 的動作放在 `loadScores()` 裡、用**剛抓回來的** imdb id，**不要**回頭去讀舊的 `m:` 快取拿 id——
舊的 `m:` 可能已經被 `sweep()` 淘汰掉，那樣就永遠清不到分數。

### 20. 顏色與觸控目標的界線
- `--faint`（`#7d8798`）是為了對比度從 `#6b7484` 調亮一階的：它正好用在 11.5px 的 `.hintline` 與 `.votes`，
  也就是「防止把 TMDB 分數誤讀成綜合分」那三層防呆的第 3 層。**最該讀得清楚的字不可以最淡。**
  其餘顏色照定案 demo，不要順手「統一」。
- 除了最近查詢的 ✕（命中區已放大到 38×38），其餘 <44px 的觸控目標是**照老闆已確認的定案 demo**，
  不是漏做，不要自己放大改版面。

### 21. 未上映永遠優先於「上映中」
TMDB 的 `now_playing` 有時候會夾帶還沒上映的片。標成「上映中」是騙人的，
所以 `metaHTML()` 與詳細頁的判斷都是 `m.upcoming` 先判、`inCinema` 要加 `&& !m.upcoming`。
電影院片單另外顯示上映日（≤7 天標「本週新上映」），因為他說電影院片單有時效性。

### 22. 「我訂了哪些平台」(`hlm_mysubs`) 與「上次篩了什麼」(`hlm_pf`) 是兩件事
- `hlm_mysubs`＝事實（我付錢訂了哪些），在設定頁勾。
- `hlm_pf`＝這次片單上的篩選，在片單上臨時改。
- 規則：**從來沒手動篩過**就用 mysubs 當預設；在設定頁改 mysubs 會**立刻同步**到 pf（不然他要回片單再點一次才生效）；
  之後他在片單上臨時改 pf 不會回頭動 mysubs。

### 23. 其它幾個小決定
- **provider id 會變**：開機用 `/watch/providers/movie?watch_region=TW` 依名稱校正一次（1 次呼叫、快取 30 天），
  校正失敗就用 `js/config.js` 裡的預設 id，不影響主流程。所以看到硬寫的 id 不用緊張，它會自我修正。
- **不在字典裡的平台**（LINE TV、Hami Video、KKTV…）用 TMDB 官方 logo 圖顯示，不亂配色。
  字典裡那 8 個（Netflix / Disney+ / Prime / CATCHPLAY+ / friDay / MyVideo / Apple TV / Google TV）
  維持設計定案的品牌色塊。
- **品牌比對用「命中的關鍵字最長」的那個**（`api.js` 的 `brandKeyByName()`），不可以先命中先贏。
  否則 `Apple TV Plus` 會被 `apple` 的 `"apple tv"` 先咬走 —— Apple TV+（訂閱）就變成 Apple TV（租買商店），
  同一部片會出現兩顆都寫「Apple TV」的標籤。**`HLM_BRAND` 的鍵順序不該決定比對結果。**
  `normProvider()` 與 `syncProviderIds()` 共用這一個函式，兩邊才不會對出不一樣的答案。
- **同品牌變體要去重**：TW 的 `flatrate` 可能同時回 `Netflix` 與 `Netflix basic with Ads`，
  兩個都會配到 `netflix` 這個 key。依 key 去重、留 `display_priority` 最小的，
  不然畫面上會出現兩顆一模一樣的標籤。
- **`store.js` 的 `entryTime()` 一定要真的 `JSON.parse`**，不可以用正則撈 `"t":123`。
  正則寫法會被 `JSON.stringify` 的欄位順序牽著走，順序一變**淘汰邏輯會靜默失效**（不報錯，只是永遠砍錯東西）。

### 24. localStorage 全部包 try/catch
無痕模式與部分 in-app 瀏覽器碰 localStorage 會直接丟例外。失敗時退回**記憶體**，
**不可以讓 App 白畫面**；設定頁會明講「這個瀏覽器不讓我存資料」。

### 25. PTT 鄉民評價：資料管線的界線（2026-08-23）

**架構**：GitHub Actions 每天爬 PTT 電影板 → 產 `data/ptt-movie.json` → App 讀同源 JSON。
純靜態網頁不能直接抓 PTT（跨網域被擋），這是唯一不用養伺服器的做法。**不要改成前端直接抓。**

- **只數標題的 `[好雷]/[普雷]/[負雷]`，不讀內文、不接 AI**（老闆確認過的範圍）。
- **`scripts/ptt-parse.mjs` 是唯一放 PTT 網頁結構假設的地方**（`SELECTORS`）。
  改版只改那裡；`fetch-ptt.mjs` 只管抓網頁與輸出，不可以自己另外寫 HTML 解析。
- **⚠️ 這些結構假設沒有對著真實 PTT 驗過**——開發機的網路出口擋掉 `www.ptt.cc`。
  測試用的是 `test/fixtures/ptt-list-*.html`（我們自己手寫的假 PTT 頁面），
  **測試全綠只代表解析邏輯對得上我們假設的結構，不代表對得上真實 PTT**。
  真正的驗證在 GitHub Actions 第一次跑的時候。

**死規定（PM 明訂，不可以放寬）**：抓到 0 篇文章、或 0 篇帶得到雷標籤，
就 **exit 1 讓 Actions 紅掉**並印診斷（URL、HTTP 狀態、HTML 前 500 字）。
理由：安靜產出一份空 JSON ＝ App 顯示「PTT 上沒找到討論」＝ 沒有人知道爬蟲死了。
`healthCheck()` 就是這條規則，`ptt.yml` 也不可以加「失敗也繼續」。

**片名比對：寧可漏抓不要錯配。** 錯配的後果是「這部片的評價其實是別部片的」，比沒有更糟。所以：
- 短片名（2～3 字）只認出現在標題**前段**的（`HEAD_CHARS`），不然「怪物」這種兩字片名到處都中。
- 英文別名至少 5 個字母且要**整個單字**命中——`Dune`（4 字母）故意不用，**這是已知的漏抓，不是 bug**。
- **歧義守衛**：最高分有兩部以上不同的片就放棄（例：片單同時有「沙丘」與「沙丘：第二部」，
  標題只寫「沙丘」時不配給任何一部）。
- `Re:` 回文一律排除：**回文的標籤是沿用原文標題的**，不是回文者自己的評價，算進去會重複計票。
- `[微好雷]`、`[無雷]` 這種變體不收（只認開頭的 `[好雷]/[普雷]/[負雷]`，容忍 `[ 好雷]`、`[好 雷]`、全形括號）。

**其它幾個刻意的決定**：
- 判斷文章新舊用**網址裡的 unix 時間戳**（`M.1787184000.A.xxx.html`），不用列表上的 `M/DD`——
  那個沒有年份，跨年會判錯。
- **只有內容真的變了才寫檔**（`payloadEquals` 比對時**忽略 `updated`**），
  不然每天都會產生一個「其實沒變」的 commit。附帶效果：沒變的那幾天 `updated` 會停在舊時間，這是刻意的取捨。
- `buildPayload` 的排序（推文數 → 網址）是為了**輸出穩定**：順序抖動也會製造假 commit。
- 每次請求之間睡 700ms、帶可辨識的 User-Agent、不並發。**這是別人的免費服務**，不要為了跑快改掉。
- 電影板照理說不用 `over18` cookie，但**被導到年齡確認頁時會帶 cookie 重試一次**（防呆，沒實測過）。
- TMDB 金鑰只從 `process.env.TMDB_KEY`（Actions secret）讀，
  **所有輸出都經過 `redact()`**，錯誤訊息只印 API path 不印帶 key 的完整 URL。

**同名破平（2026-08-23 第一次真跑之後補的）**：最高分打平時**不要直接放棄**，
依序用 ① 現正上映（`now_playing` 來的）② TMDB `popularity` ③ 上映日較新 來挑一部，三個都一樣才放棄。
> 為什麼：第一次真跑，12 篇歧義放棄**全部是蜘蛛人**——片單裡有好幾部蜘蛛人，
> 而討論度最高的那部片正在掉資料。⚠️ 這是「在同一個候選集合裡挑一部」的破平規則，
> **不是**放寬命中門檻；「寧可漏抓不要錯配」的原則沒有變，命中的標準一個字都沒降。
> Actions 紀錄會印「同名破平的例子」，那是**最該人工看一眼**有沒有配錯的地方。

**`scanned` 的六個欄位（契約，不要自己加減）**：
`pages` 抓幾頁／`posts` 掃到的文章總數（**含 `[討論]` 等非雷文**）／`tagged` 雷文總數／
`matched` 比對到／`ambiguous` 破平也分不出來而放棄／`unmatched` 根本沒命中。
**`tagged = matched + ambiguous + unmatched`**（PM 讀健康度時不必心算，這是他要求的）。

### 26. PTT 鄉民風向（畫面）— 2026-08-23

規格是 lab-ux 的 `movie-ux-spec.md` §9，老闆看過 demo 說「很不錯」。以下是**做了就別亂改**的部分：

- **不給 PTT 一個分數**：不換算 0–100、不進綜合分數環，只有比例條＋票數＋一句結論詞。
  換算成分數會讓它變成第五個評分網站，還會誘使人拿去跟 IMDb 比。
- **位置在四個分數之後、平台之前**：兩者都在回答「值不值得看」，平台回答的是「我打不打得開」。
- **`n < 4` 不畫比例條也不給結論詞**（3 篇算出「67% 好雷」是說謊）。
- **好雷率的分母含普雷**，而且一定要帶「好雷率」三個字（裸百分比會被誤讀成評分）。
- **「保底異見」是刻意的**：預設 5 則 ＝ 推文前 4 則 ＋ 強制撈一則相反立場。
  所以**第 5 則的推文數會比第 4 則低**，這不是排序 bug，**不要改成純推文排序**。
  一面倒的片會讓少數派永遠被擠掉，但「有沒有人覺得雷」正是這個 App 的名字在問的事。
- **標題單行截斷**是功能（版面＋降低爆雷），不要改成 2 行 clamp；不做遮蔽／模糊（要判斷有沒有雷只能讀內文或 AI，老闆不做 AI）。
- **「沒有討論」與「讀不到」必須長得完全不一樣**：前者一行灰字、後者錯誤訊息＋44px 重試鈕。
  混在一起會讓人以為冷門片壞掉、或以為爬蟲掛了只是沒人討論。**404 走「讀不到」，不是「沒討論」。**
- **過期判定用 JSON 裡的 `updated`，不是抓取時間**（門檻 3 天）：
  爬蟲掛掉時 fetch 會成功、`updated` 卻是舊的，那正是要抓出來的情況。
- **`data/ptt-movie.json` 不進 `sw.js` 的殼快取**（`FILES` 裡沒有，而且 SW 對它直接放行）。
  殼快取的名字綁著 `HLM_VER`，進去就會被凍到下次改版——症狀是「老闆看到的永遠是舊風向，而且沒人發現」。
  離線的退路是 `localStorage` 的 `hlm_ptt` 副本（**network-first，失敗才吃副本**）。
- **整份 JSON 一次抓、一個 session 只抓一次**（`api.js` 的 `pttMemo` ＋ `app.js` 的 `ptt.data` 兩層都有擋，
  兩層都有測試在守）。檔案**超過約 300KB 就要回報**，屆時得拆檔（目前 10KB）。

### 27. ⭐ PTT 文章網址：兩端各一道白名單（2026-08-23 QA 退件 F-1）

`data/ptt-movie.json` 的 `url` 是**外部輸入**（PTT 網頁 → 爬蟲 → 檔案 → App 渲染成可點的連結）。
`esc()` 只擋得住「把 HTML 屬性撐破」，**擋不住 scheme**——所以三道關卡缺一不可：

| 位置 | 做什麼 | 不做的後果 |
|---|---|---|
| `scripts/ptt-parse.mjs` 的 `absUrl()` | 只認 `ptt.cc`（含 http→https 升級），其餘一律回 `null`，該篇整篇丟掉 | PTT 列表頁只要出現一個絕對網址的標題連結，就會被寫進資料檔 |
| `scripts/ptt-parse.mjs` 的 `buildPayload()` | 寫檔前再擋一次 `PTT_URL_RE` | 有人繞過 `absUrl` 塞 entries 就中 |
| `js/ui.js` 的 `pttPostHTML()`（`PTTOK`） | 不合格就**不輸出 `<a>`**，退成 `.pttpost.nolink` 純文字列 | `javascript:` **會在本站 origin 上執行**；`data:`／外站網址變成釣魚跳板 |

- 三道是**各自獨立**的（各有各的毒測資與突變，拿掉任何一道都會紅），不是「一層在守一層備援」。
- **不合格的網址是「不能點」，不是「整則消失」**——標題還是要看得到（漏一則評價比較不痛）。
- 標題／`tag`／`date` 也全部要 `esc()`。**`<b>` 是圖例自己在用的合法標籤，不可以拿來當 XSS 判準**（會誤報）。

**⭐ 白名單／驗證類的程式碼，突變清單要有四種**（2026-08-23 QA 第二輪：他打的是
「白名單**寫得不對**」而不是「白名單**不見了**」——**這才是安全性程式碼真實的退化方式**，
沒有人會整段刪掉它，但很有人會順手改一下正則）：

| # | 突變 | 這個專案的例子 |
|---|---|---|
| ① | 整段移除 | V04／V05 |
| ② | **錨定符號拿掉**（`^`／`$`） | W6／W8 ← `https://evil.example.com/?x=https://www.ptt.cc/` 就過了，**唯一真的可利用的** |
| ③ | **跳脫字元拿掉**（`\.` → `.`） | W9 ← `www.pttxcc/` 就過了 |
| ④ | **條件放寬一格**（`https` → `https?`、`===` → `>=`） | W7 |

配套：毒測資清單要跟著長出「字串裡含合法網域」與「換一個 scheme」這兩種，
**而且三道關卡要各自餵一次**（QA 抓到我第 3 道的毒清單只有 4 種簡單的）。

**另一個耐錯的模式（QA 肯定、要保留）**：`absUrl()` 是**改寫成 canonical 形式**
（`PTT_ORIGIN + path`）而不是「通過就原樣放行」。所以就算點號跳脫寫錯，
輸出仍然是 `https://www.ptt.cc/...` 開頭，炸不了。**驗證＋改寫 > 驗證＋放行**，
以後寫任何 URL／路徑白名單都優先用這個形狀。

> 🔁 **這條是通用教訓，不只這個專案**：
> **「只允許 X」的斷言，如果測資裡沒有任何一筆「不是 X」，那條測試是空轉的。**
> 我原本寫了 `每個 <a> 都指向 ptt.cc`，但 fixture 裡的網址本來就全是 ptt.cc ——
> 程式裡沒有任何一行可以被拿掉讓它變紅。寫完這種斷言就問自己：**有沒有一筆毒測資？**

### 28. 鑰匙圈（跨 App 身分）— 2026-08-23

一組密碼解開他所有 App 的金鑰。模組正本在 **`xd1104/keyring`**，我們這邊是
`js/keyring-unlock.js`（**複製品，不可以在這裡改**；t13 會用 SHA-256 跟正本比對）。

- **`appId` 是 `movie-library`**（ASCII，跟 repo 同名）。鑰匙圈的鐵律：id 不可以用中文，
  中文會在不同系統之間被正規化成不同字串而分裂成兩筆。
- **⭐ 一個 App 只吃一把金鑰，我們有兩把 → 打包成 JSON blob**（PM 拍板）：
  後台那一格放 `{"tmdb":"…","omdb":"…"}`，`tokenKey` 指到**專用的新 key** `hlm_keyring_blob`，
  我們自己解析後才寫進 `hlm_key_tmdb` / `hlm_key_omdb`。
  **絕對不要把 `tokenKey` 直接指到 `hlm_key_tmdb`**——那樣 OMDb 永遠進不來，
  而且鑰匙圈「換人／被收回」時會把他**手貼**的金鑰一起清掉。
  （讀過模組原始碼：`CFG` 是模組層級的單例、`writeToken()` 只認一個 key，
  所以「同一個 App 開兩個 keyring 實例」是做不到的，blob 是唯一乾淨解。）
- **blob 貼錯是主流程不是邊緣案例**（他是手貼的）：`Api.parseKeyringBlob()` 失敗要丟
  `keyringbad`，設定頁用 `human()` 顯示人話**並直接印出正確格式長怎樣**。
- **兩條路永遠並存**：鑰匙圈解不開（忘記密碼／被收回／離線）時，手貼金鑰那條路要還在。
  沒有 `Keyring` 這個全域時，設定頁**不提**鑰匙圈（不要給他一條走不通的路）。
- **順序：`setupKeyring()` → `boot()` → `setupSW()`**。鑰匙圈要先跑，`boot()` 的自我體檢才不會
  誤判成「還沒設定」；但它整段包 try/catch——**模組壞掉頂多退回手貼，不可以讓 App 打不開**
  （跟第 18 條同一個精神）。
- **⭐ 沒勾「記住這台裝置」時，兩把金鑰要跟著 blob 一起進 `sessionStorage`**。
  模組的 `writeToken()` 就是這樣分的；我們如果照舊寫 localStorage，
  「借別人手機用一下」會把 TMDB／OMDb 金鑰**永久留在那台裝置上**。
  `S.keys()` 因此改成 session 優先、localStorage 其次；兩邊都用 JSON 編碼（別一邊原文一邊 JSON）。
- **`hlm_keys_src` 記來源**：只有 `"keyring"` 的才會在鎖回去時被清掉，**手貼的不准動**。
- **視覺零設定**：解鎖畫面是跨 App 公版。**`css/app.css` 裡一條 `kr-` 規則都不可以有**（t13 會擋）。
  身分藥丸由模組產生 HTML，我們只給位置（`#krslot` 首頁 footer、`#krslot2` 設定頁）。
- 這個 App **沒有「寫入動作」要守門**，所以不用 `Keyring.open()`；只用
  `init()` / `maybeIntro()`（首頁畫完）/ `chipHtml()`。

#### ⭐ 接共用模組的驗法（2026-08-23 QA 退件 K-1，這條比什麼都重要）

**要問的不是「我有沒有包 try/catch」，是「`boot()` 這條路上總共碰了模組幾次」。**
我第一版包住了 `setupKeyring()`，五個存取點守了四個——漏的 `Keyring.isUnlocked()`
不在 `setupKeyring()` 裡，而在 `renderSetup()` 裡，隔了兩層呼叫，而 `renderSetup()` 是
`boot()` 直接叫的、`boot()` 不在那個 try/catch 裡面。症狀：模組版本落差（少了那個方法）
＋ 第一次使用 → **整支 app.js 停掉、設定頁畫不出來、連手貼金鑰的逃生門都不見**。
肉眼看那段程式碼是「有包啊」，所以**一定要用機械的方法驗**：

```
grep -rn "Keyring\." js/*.js        # 列出全部存取點（目前 5 個）
```

每一個存取點都要做四種樁：**①模組整支不存在 ②少這個方法（版本落差）③這個方法 throw
④連讀屬性都 throw**，而且**每一種都跑一次完整開機**，斷言四道逃生門：
進得了片單／搜尋有反應／**進得了設定頁**／手貼存得進去（外加「完全沒金鑰時引導畫面畫得出來」）。

t13 §55 就是這個迴圈（資料驅動）：以後多呼叫模組一次，只要在 `POINTS` 加一行，四種壞法自動跟著跑。
程式碼這邊也統一走 `krTry(fn, fallback)`，唯一的例外是 `Keyring.init`——
它的守衛是啟動那段的 try/catch，兩層都包的話那層會變成沒人守得住的死碼（K07 也會失去意義）。

> 🔁 **通用**：任何「複製進來的共用模組」都適用。共用模組最常見的退化不是「不見了」，
> 是**正本改了 API 而你這份沒跟上**；而它一定是**同步擲出**，所以會停在你意想不到的那一層。

#### 兩種金鑰來源交錯（`saveKeys` / `saveKeysFromKeyring` / `clearKeyringKeys` / `keys()`）

這四個函式之間是**狀態機**，只測單向會漏掉四件事（QA 抓到我只測了單向）：
寫任何一邊都要**把另一邊清乾淨**（`saveKeys` 要 `ssDel`、不記住的解鎖要 `del`），
手貼要把 `hlm_keys_src` 清掉，`keys()` 是 **session 優先**（層 B 的備援，要單獨驗）。
t13 §57 跑完整序列「手貼 → 鑰匙圈(記住) → 鑰匙圈(不記住) → 手貼 → 收回 → 再解鎖再收回」，
**每一步都斷言 localStorage 與 sessionStorage 兩邊各有什麼**。

#### 「解鎖了」≠「金鑰進來了」

後台還沒填「好雷嗎」那一格就先解鎖，**很可能是他遇到的第一個狀態**。
設定頁的文案有**三種**（未解鎖／解鎖且有金鑰／解鎖但那格是空的），
第三種要明講「鑰匙圈解開了，但裡面還沒放這個 App 的金鑰」＋下一步，
**不可以說「下面那兩格會自動填好」**——那時候兩格是空的。

### 29. v1.3.0：金鑰改吃鑰匙圈的**公開值**，沒有登入畫面也沒有設定金鑰那一頁

老闆要的結果：**打開網址就能用**。這個 App 用的是 TMDB／OMDb 的免費查詢金鑰，不是憑證，
所以鑰匙圈那邊把 `movie-library` 標成**公開**（值以明文放在 `keyring.json` 的 `apps[]`，
`public:true` ＋ `plain`，不綁使用者、不需要密碼）。模組拿到之後寫進 `hlm_keyring_blob`，
我們照舊拆成 `hlm_key_tmdb` / `hlm_key_omdb`。

**拿掉了什麼**（都是刻意的，不要加回來）：
- **第一次使用的「設定金鑰」引導頁**：`boot()` 不再因為沒金鑰就 `renderSetup(true)`。
- **身分藥丸與自動彈解鎖**（`chipHtml()` / `maybeIntro()`）：公開模式沒有身分可言；
  在「後台還沒設成公開」的過渡期，使用者該看到的是一句人話，不是一個解不開的登入框。
  → 少兩個存取點 ＝ 模組壞掉時少兩條會炸的路。
- 設定頁的手貼金鑰表單。

**⭐ 但逃生門一定要在（QA 連兩輪抓到的都是這件事）**：
- 拿不到金鑰時，**首頁片單區**顯示「現在還不能查片」＋原因＋**手貼金鑰表單**＋「再試一次」。
- **`ui.js` 完全不碰 `Keyring`**（只吃布林值與字串，`krErrCtx()` 幫它算好）。
  模組壞掉時連錯誤畫面都畫不出來的話，那才是真的被鎖在門外。t13 §54 有靜態斷言在守。
- **手貼的金鑰永遠優先於公開值**（`S.keysManual()`）：他特地貼的最大，公開值不可以蓋掉。
  設定頁只在「他真的手貼過」時顯示一行狀態＋「清掉，改用鑰匙圈的」（不顯示明文，只有遮罩）。

**存取點剩 3 個**（`grep -rn "Keyring\." js/*.js`）：`init` / `whenReady` / `reload`。
t13 §55 的樁矩陣照舊：每個點 × 四種壞法（不存在／少這個方法／一叫就爆／連讀屬性都爆），
每一種都要跑完整開機並斷言「片單出得來／搜尋有反應／進得了設定頁／逃生門畫得出來／
按了重試還回得來」。**以後多呼叫模組一次，就在 `POINTS` 加一行。**

**t13 的 SHA-256 比對**（我們這份 `js/keyring-unlock.js` 必須跟正本一模一樣）**不可以放寬**。
正本現在是可寫的那份 clone（`/home/user/keyring/client/keyring-unlock.js`）；
`xd1104/keyring` 那個唯讀 clone 已經落後。模組改版就重新複製一次，不要改比對。

### 30. v1.4.0：動效基調（沉穩）＋ 開場畫面（印記）— 2026-08-25 定案

範本正本在 `app-template/motion/`（已上線、已 QA 放行）。這支 App 只搬「會動的那一層」。

#### ⛔ 絕對不要整包套範本的 `motion.css`
範本那份帶著自己的色票，而它的 **`--bg` / `--surface` / `--muted` / `--line` 跟 `css/app.css` 撞名**
——`<link>` 進來就會把「好雷嗎?」的深色配色整個蓋掉，變成範本的樣子。
所以 `css/motion.css` 是**新寫的**：只有 15 個動效 token（數值逐字照範本的沉穩那組）
＋ 綁在這支 App 自己 class 名稱上的規則。
`t14 §65` 有**白名單斷言**：`motion.css` 只准宣告那 15 個變數，多一個就紅；
`§66` 斷言它一個色碼都沒有。**要同步範本的新版動效時，一樣只搬 token 與規則。**

#### 動效只做這五項（老闆「看得出差別」的那幾個），不要發散
開場／按下回饋／搜尋結果與片單錯開進場／載入骨架屏／`#view-home ↔ #view-detail` 的 push-pop。
**不因為動效而新增任何按鈕或連結**（「只查評價、不導購」沒有變）。

- **按下回饋是全 App 每一個可點元素都要有**，不是挑幾個。`t14 §68` 用 jsdom 把首頁／詳細頁／設定頁
  的可點元素**全掃出來逐一 `el.matches()`**，不是列白名單（上一輪範本就是列白名單漏了第 5 顆）。
- **進場動畫一律 `backwards`，不可以用 `both`／`forwards`**：`both` 會永久保留最終 transform，
  優先度高於一般宣告 ⇒ `:active{transform:scale()}` 從此失效、按鈕變成死的，而且純功能測試抓不到。
  `t14 §67` 掃 `motion.css` 每一條 `animation:` 簡寫在擋這件事。
- **push/pop 的 20px 橫向位移用 `#app{overflow-x:clip}` 收掉，不可以改成 `overflow-x:hidden`**：
  hidden 會讓 `#app` 變成捲動容器，裡面的 `.stickyhead` / `.dtop`（`position:sticky`）就會黏錯對象。
  （2026-08-25 用本機真 Chrome 實測：`overflow-x:clip` 下捲到 scrollY=157 時 `.stickyhead` 的
  `getBoundingClientRect().top` 仍是 **0**，橫向溢出最大值 **0px**。）
- 返回時首頁的 `.pop` 是「拿掉 class → 讀一次 `offsetWidth` 強制回流 → 加回去」重播的，
  **不要改成用計時器移除**：動畫是 backwards fill，跑完不留 transform，本來就不需要收尾。

#### 開場（`js/splash.js` ＋ `css/splash.css` 是範本複製品，不可以在這裡改）
`t14 §74` 用 SHA-256 跟 `app-template/motion/` 比對（跟第 28 條的鑰匙圈模組同一個精神）。
改法是回範本改，再複製過來。**特別是 `onColor()`：它跟鑰匙圈後台的預覽縮圖是同一份，分岔了
Benson 在後台看到的就跟實機不一樣。**

- **落地值只有 `index.html` 裡那一小塊**：`--splash-bg:#0b0d12` / `--splash-ink:#f3f5f9` /
  `--splash-accent:#ffc14d` ＋ `SPLASH_CONFIG`（appId `movie-library`、glyph `雷`、name `好雷嗎?`、
  tagline `一頁看完值不值得看`）。
- **`--splash-bg` 必須逐字等於 `manifest.background_color` 與 `<meta theme-color>`**。
  剛好三處本來就都是 `#0b0d12` ⇒ **manifest 完全不用改，他也不必把 App 從主畫面移除重加**
  （已用 `t14 §60` 機器比對過，不是用看的）。不一致的症狀是「iPhone 從主畫面開會白閃一下」，
  在電腦上永遠看不到、也不會有人回報。
- **符號的文字色不是設定項**，由 `onColor(accent)` 算（白字／深字取對比高的那個，最差有下界）。
  `t14 §72` 拿 `#00d038`（舊的亮度門檻算法在這裡只有 2.08:1）、`#438c83`（全色域最差）等
  毒色碼**走真的程式路徑**驗對比 ≥3:1。**不要把它加成設定項，也不要「簡化」成亮度門檻。**
- **只在冷啟動播**（`sessionStorage`）。切分頁、返回、從詳細頁回來都不重播。
- **鑰匙圈的 `apps[].splash` 是快取優先、背景更新、下次冷啟動才生效**——
  中途換字比晚一次生效難看得多。`t14 §73` 斷言 `applyLook` 在整支 splash.js 裡只被呼叫一次。
  fetch 失敗（沒網路／壞 JSON／沒登記）一律無感、保留舊快取。

#### ⭐ 呼叫端的兩件事，缺一不可（範本那輪 QA 實測過的災情）
1. **`sw.js` 的 `SHELL` 要預快取 `css/motion.css` / `css/splash.css` / `js/splash.js`**。
   `t14 §63` 會**全掃** `index.html` 引用到的每一個本站 css/js，逐一驗它在不在 `FILES` 裡。
2. **`app.js` 一律寫 `window.Splash && …` ＋ `try/catch` ＋ `splashFallback()`**，
   絕對不可以裸寫 `Splash.hold()`：那支檔案載不到時（離線、部署漏檔）會丟 `ReferenceError`
   ⇒ **整支 app.js 的 IIFE 當場中止 ⇒ 0 筆資料、沒套樣式的開場永遠卡在螢幕上，
   而保險絲就住在那支沒載到的檔案裡**。
   `splashFallback()` 除了收掉 `#splash`，還要補上 `touchstart` 監聽——
   平常那行在 splash.js 裡，沒有它 iOS 的 `:active` 全部是死的。
   > 順帶：`test/harness.mjs` 本來就會把所有 `<script src>` 拿掉，
   > 所以 **t1～t13 每一支都是在「沒有 Splash」的情況下跑的 ＝ 天然的負控組**。

`splashReady()` 掛在每一條「畫面畫好了」的出口（片單成功／失敗／逃生門／詳細頁三條／設定頁），
**失敗也要叫**，不然開場會變成當機畫面、要停到 6 秒保險絲才走。

#### ⚠️ `test/mutate.mjs` 在這台 Windows 機器上本來是壞的（2026-08-25 修好）
兩個問題，**都會讓它謊報**：
1. `hashAll()` 的 `cd ${ROOT}` 沒有引號 → 路徑含空格（`Claude Work`）時 `bash: cd: too many arguments`
   → 整支跑不起來。
2. 工作區是 **CRLF**（Windows 的 `core.autocrlf`），而突變表裡的目標字串一律寫 `\n`
   → **127 條裡有 50 條被判成 stale**，而 stale 依這支工具自己的鐵律①就是
   「那件事現在沒有任何人在驗」。很容易被誤讀成「重構改壞了」而去改突變表（改了反而全錯）。
   → 現在比對與套用都在 LF 空間做，寫回檔案前換回原檔的換行風格；還原寫的是原始位元組，
   SHA-256 驗證照舊。另外加了一條**自證**：突變後的內容跟原檔一模一樣就直接 exit 2
   （「改壞了測試還是綠的」跟「其實根本沒改到」必須分得出來）。

**`EXEMPT_COUNT` 仍然是 3**（R1a／V09／對照組）——這一輪新增的 15 條動效突變**全部預期會紅**。

#### 這一輪突變測試自己抓到的兩件事（留著當範例）
1. **X15（`defer`）第一次是綠的**：我原本寫 `!/<script src="\.\/js\/splash\.js"[^>]*(defer|async)/`，
   而突變產生的是 `<script defer src="…">`——**屬性順序一換就繞過去了**。
   正解是先抓整個標籤（`<script[^>]*src="…"[^>]*>`）再驗屬性。
   127 條裡唯一沒守住的就是它，靠工具抓出來的，不是靠讀程式碼。
   🔁 通則：**「X 後面不准接 Y」這種斷言，遇到屬性／參數順序可換的語法一定要改成「先取整體再驗」。**
2. **`css/splash.css` 是範本複製品，它的預設值是「範／App 範本／#241f1b」**。
   「`splash.js` 載不到但 `splash.css` 載到了」的那一次（第一次進站、還沒有 SW、網路不穩），
   畫面上就會出現**別人家的品牌**。所以 `index.html` 的 `<style>` 連
   `--splash-glyph` / `--splash-name` / `--splash-tagline` 也一起蓋掉，
   `t14 §61` 用**全掃描**（把 splash.css 宣告的每一個 `--splash-*` 抓出來逐一驗有沒有被蓋）在守，
   唯一的例外是 `--splash-on-accent`（刻意由 `onColor()` 算，不是設定項）。

---

### 31. v1.4.1：白閃修正（關鍵路徑 CSS）＋ 開場最短顯示 950ms — 2026-08-26

Benson 真 iPhone、從主畫面開 PWA 回報：「開電影評分的時候會**先閃一下白色**，然後才開始播開場動畫。」
另外提「開場動畫有一點點太快，想要長一點點」。

#### 診斷結論（用本機真 Chrome `--headless=new` ＋ CDP **逐幀取樣**量的，不是 `--dump-dom`）

**白閃不是 v1.4.0 改出來的，但 v1.4.0 讓它變得刺眼，而且確實多了幾十毫秒。**

對照組是 `e665911`（套動效之前，`<head>` 只有 1 支 CSS）。同一台機器、同一支 Chrome、
同一個 Chrome 行程內交錯取樣（每個版本 17 次、順序每輪隨機打散），走 SW 殼快取：

| CPU 節流 | 對照組 e665911 | v1.4.0 | v1.4.1（改後） |
|---|---|---|---|
| 不節流，first paint 中位 | 201ms | 204ms | 206ms |
| ×6，first paint p25 / 中位 | 224 / 248ms | 316 / 302ms | 168 / 311ms |

- **「導航開始→第一次繪製」這段 WebView 是白的，這件事對照組一樣有**（不節流也要 200ms）。
  真正的白閃來源是**這一段**，而它主要花在 SW 派送 index.html ＋ 解析整份 HTML ＋ 首次版面計算，
  跟我們加的三支 CSS 沒什麼關係。
- v1.4.0 比對照組**慢 ~50ms（CPU×6）**，那是多兩支 CSS 要解析 ＋ 開場那幾個元素的樣式/版面成本。
- **第一次繪製本來就是深色**（每一次取樣、每一個版本，首幀角落都是 `#0b0d12`）——
  因為 CSS 是 render-blocking，不可能在 CSS 到位前畫。
- ⚠️ **但那個「一定是深色」完全靠外部檔案**。實測：**把三支 CSS 全部改成 404，
  改之前的首幀是 `#ffffff`（純白），改之後是 `#0b0d12`。** 這才是這一版真正修掉的東西。

#### ⭐ `<head>` 最前面那塊「關鍵路徑 CSS」（新增，別亂搬）

`index.html` 現在有**兩塊 `<style>`**，位置都是刻意的：

| 位置 | 內容 | 為什麼一定要在那裡 |
|---|---|---|
| 所有 `<link>` **之前** | 關鍵路徑：`html` 底色、`#splash` 蓋滿、`.sp-*` 的靜止第一幀 | 外部 CSS 一支都沒到位時（離線、SW 殼快取沒建完、部署漏檔）第一次繪製仍然是深色的開場，不是純白 |
| `css/splash.css` **之後** | 落地值 `:root{--splash-*}` | `splash.css` 是範本複製品，`:root` 預設是範本自己的品牌，同權重後宣告者勝。**搬到前面去就會被「範／App 範本／#241f1b」蓋掉**——那正是第 30 條 `t14 §61` 存在的理由 |

- 關鍵路徑那塊**顏色一律走 `var(--splash-*, 後備字面值)`**，不可以只寫死色碼：
  值仍然由落地那塊 ＋ `splash.js`（鑰匙圈換色）決定，後備值只有「連 splash.css 都沒載到」才用得到。
- **符號字色只「引用」不「宣告」**：`color:var(--splash-on-accent)`。
  值是 `onColor()` 算的（第 30 條）。在這裡寫死一個色碼＝同一條規則活在兩個地方，換色必分岔，
  而且分岔的那一份沒有對比度下界。`t14 §75` 有斷言在擋（X20 突變會紅）。
  > 附帶：`t14 §61`／`§72` 對 `--splash-on-accent` 的判準從「這個名字不准出現」精確化成
  > **「不准有宣告（`--splash-on-accent:`）」**——它要守的是「不是可調的旋鈕」，引用不會讓它變成旋鈕。
  > 同時那兩條都改成**掃全部 `<style>` 區塊**，不是只看第一塊（現在有兩塊了）。
- 關鍵路徑那塊**不可以有 `@import` 或 `url()`**，否則等於沒脫離關鍵路徑（`t14 §75` 在擋）。
- `.sp-ring` 刻意**不進**關鍵路徑：它是第二拍的光環，靜態 `opacity:0`，第一幀本來就不該出現。
  這是 `t14 §75` 唯一的豁免，名單長度有斷言（擋人偷加）。

#### ⛔ 已經評估過、**刻意不做**的三件事 —— ⚠️ **第 1、3 條在 v1.4.2 被推翻，看第 32 條**

1. ~~**把三支 CSS 改成非阻塞**：實測 first paint 完全沒有變快~~
   → **只有「單獨做」的時候沒變快**，因為那時 `<head>` 裡的同步腳本 `js/splash.js` 還在擋。
     兩件事一起做才有效（第 32 條：冷啟動 Slow-3G 2670ms → 1150ms）。
2. **把 `<link>` 移到 `<body>` 的 `#splash` 之後**（讓瀏覽器先畫開場再解析樣式表）：
   實測只快 ~20–35ms，代價是**第一次繪製會變成兩段**（先只有 inline 的靜止方塊、外部 CSS 到了才補上細節），
   在 iOS 上驗不到，風險大於收益。**這條仍然成立。**
3. ~~**`js/splash.js` 改 `defer` 或延後**：實測 first paint 一點都沒變（它不是瓶頸）~~
   → **那個數字是在「走 SW 殼快取的熱啟動」量的，只對熱啟動成立。**
     冷啟動（沒有 SW、快取全清）＋慢網路下它是**最大的一塊**（第 32 條）。
     「不可以中途換字」那條鐵律仍然成立 —— 要動它的形態必須先把「讀快取寫 CSS 變數」那一小段搬到更前面。

> 🔁 通則（v1.4.2 修正）：**「head 上有幾個 render-blocking 資源」不等於「first paint 有多慢」，
> 但那句話只在「頻寬不是瓶頸」時成立。** 上一輪的量測全部走 localhost ＋ SW 殼快取 ⇒ 下載是免費的，
> 所以看起來什麼都動不了。一旦條件換成「Safari 第一次開這個網址」（沒有 SW、沒有快取、行動網路），
> first paint 就完全被「關鍵路徑上要下載多少位元組」決定。
> **量效能的結論一定要連著「在什麼條件下量的」一起寫，不然下一輪會被自己的結論擋住。**

#### 開場最短顯示 `MIN_SHOW` 650ms → **950ms**（Benson 2026-08-26 拍板）

理由寫在 `js/splash.js` 第 70 行：動作本身約 640ms 就收尾（`--sp-hold` 220 ＋ 動作 420），
650 等於「動作剛做完就走」，沒有一拍讓人看清楚 ⇒ 多留約 300ms 的定格。
**要的是「多停留」不是「變慢」——`--dur-*` 與 `--sp-hold` 一個都不要動。**

- 這是**改在範本正本** `app-template/motion/splash.js`，再同步過來的（`t14 §74` 在比對）。
  `app-template/CLAUDE.md`、`motion/README.md`、lab-ux 的 `motion-spec.md` 都同步改了。
- `t14 §71` 的釘法：①`state().minShow === 950` 直接讀常數（不用時間推）；
  ②**800ms 時開場必須還在畫面上**（650 的話那時已經在收場尾巴了）。突變 X19 改回 650 會紅。
- ⚠️ `t14 §73` 讀鑰匙圈那幾條的 `KR_WAIT = 2100` **跟著 MIN_SHOW 走**
  （950 ＋ 收場 400 ＋ afterSplash 400 ＝ 1750 才會有結果）。**改 MIN_SHOW 要回來看那一行。**
- 實測開場停留（`#splash` 從出現到從 DOM 消失）：**650 版 1040ms → 950 版 1340ms**（快網路、走 SW），
  CPU×6 時兩版都是 ~2.4 秒（那時是資料在等，B 模式本來就該蓋住）。

#### 這一版的量測工具（要重驗就照抄）

**`scripts/probe/`（已收進 repo，不是暫存區）**：`server.mjs`（靜態站，**一定要另一個行程**）、`cdp.mjs`（Node 22 內建
WebSocket 的極簡 CDP 客戶端）、`png.mjs`（極簡 PNG 解碼，只為了讀首幀像素）、
`bench.mjs`（同一個 Chrome 內多目標**交錯＋每輪隨機打散**取樣）、`shot.mjs`（截圖取樣座標）。

⭐ **量之前先修尺**：第一版量法（每次 relaunch 一個 Chrome、固定順序）得到的結果
**同一組比較跑兩次順序會相反**（一次說 v1.4.0 快、一次說對照組快）。
兩個雜訊源：①headless Chrome 第一個網路請求要等 network service 起來（實測 ~570ms，
要先拿一個不相干的檔暖機把它燒掉）；②**目標的先後順序有系統性偏差**（第一個跑的那個特別慢）。
修成「單一 Chrome ＋ 交錯 ＋ 每輪隨機打散 ＋ 看 p25 不只看中位」之後，結果才穩定可重現。

---

### 32. v1.4.2：body 腳本一律 `defer` ＋ 白閃的真正瓶頸查清楚了 — 2026-08-26

新事證：**Benson 用 Safari 開同一個網址（不是主畫面圖示）一樣會閃白** ⇒ 這不是 iOS PWA
系統啟動圖交接的問題，是**純粹的網頁層問題**：第一次繪製之前，瀏覽器畫布就是白的。

#### 量測條件（第 31 條那一輪的數字為什麼不算數）

第 31 條全部在「桌機 localhost ＋ 走 SW 殼快取 ＋ CPU 只節流到 ×6」量的 ⇒ **下載是免費的**，
所以每個候選都看起來沒用。這一輪 `bench.mjs` 加了 `--cold=1`：**每一次取樣前**把該來源的
SW／Cache Storage／localStorage／HTTP 快取全清掉（`Storage.clearDataForOrigin` ＋ `Network.clearBrowserCache`），
並且**每一列都印兩個自證欄位**：`SW?`（這次的文件是不是 SW 給的，冷啟動必須是 `no`）、
`下載KB`（冷啟動必須 > 0）。沒有這兩欄的話「以為在量冷啟動、其實量到熱啟動」不會有任何徵兆。

#### ⭐ 結論一：CPU 不是變因，**頻寬才是**

| 條件（冷啟動，中位 first paint） | v1.4.1 | 只加 defer | defer＋CSS非阻塞 | 全拿掉 splash.js＋CSS非阻塞 |
|---|---|---|---|---|
| CPU×1、不節流網路 | 210ms | — | — | 132ms |
| CPU×10、不節流網路 | 407ms | — | 390ms | 289ms |
| CPU×20、不節流網路 | 1354ms | — | 1265ms | 1027ms |
| CPU×4、4G（1.18MB/s） | 295ms | 277ms | — | — |
| CPU×4、Slow-3G（89.6KB/s） | **2670ms** | **2225ms** | 2240ms | **1075ms** |

CPU 從 ×1 拉到 ×20，**候選之間的名次完全沒有變**；把網路節到 Slow-3G，差距立刻放大到 1.5 秒。
⇒ 白閃的長度是**「第一次繪製要等多少位元組」**決定的，不是算得快不快。

#### ⭐ 結論二：`<head>` 的同步腳本與阻塞 CSS **必須一起解掉，各自單獨做等於零**

| 做法（冷啟動 Slow-3G，中位） | first paint | 相對 v1.4.1 |
|---|---|---|
| v1.4.1（基準） | 2670ms | — |
| 只把三支 CSS 改非阻塞 | 2417ms | −250ms（雜訊等級） |
| 只拿掉 `js/splash.js`（上限測試） | 2316ms | −350ms |
| **CSS 非阻塞 ＋ `<link>` 搬到 splash.js 後面**（完全不動 splash.js） | **2602ms** | **0ms** |
| 只把 body 那六支 js 加 `defer`（← 這一版做的） | 2225ms | −445ms |
| inline 一小段開場 bootstrap ＋ splash.js 移到 body ＋ CSS 非阻塞 | **1150ms** | **−1520ms** |
| 物理下限（2.3KB 純 HTML，只有關鍵路徑 CSS ＋ `#splash`） | 770ms | −1900ms |

原因：`<head>` 的同步腳本會把解析器**停在 `<body>` 之前** ⇒ 根本沒有東西可以畫；
而阻塞 CSS 會讓有東西也不准畫。**擋住的是兩道門，開一道沒有用。**
而且第一次繪製要等的那幾個檔，是跟 body 那 208KB 的 js **搶同一條頻寬**的
（`keyring-unlock.js` 86KB ＋ `app.js` 42KB ＋ `ui.js` 38KB ＋ `api.js` 21KB…）。

#### 這一版真的做了什麼：body 六支 js 一律 `defer`

`index.html` 的 `keyring-unlock / config / store / api / ui / app` 都加 `defer`。
它們加起來 ~208KB，而第一次繪製一個位元組都用不到（畫面完全被 `#splash` 蓋住）。
不加 `defer` 時它們是「解析器阻塞腳本」＝高優先權，會把關鍵路徑上的檔案擠到後面。

- **實測**：Slow-3G 冷啟動 2670→2225ms（−445ms，三輪重現）；4G 冷啟動 295→277ms；
  走 SW 熱啟動 227→220ms（在雜訊內，**沒有退步**）。開場畫面在 450ms／1100ms 兩個取樣點
  **逐像素與 v1.4.1 相同**；三支 CSS 全 404 時首幀仍是 `#0b0d12`。
- **`js/splash.js` 不在這一批裡**：它必須留在 `<head>` 當同步腳本（`t14 §62`、突變 X15 在守），
  否則會「先畫預設再中途換字」。
- ⚠️ **`defer` 之間保序、同步之間也保序，但一支 `defer` 一支同步就會反過來**。
  鑰匙圈晚於 `config.js` 執行的話，金鑰還沒塞回 localStorage，`boot()` 的自我體檢會誤判成「還沒設定」。
  `t13` 有一條在守「鑰匙圈與 config.js 的載入形態必須一致、而且不是 async」（突變 X22 會紅）。
- `t14 §77` 是**全掃描**：把 `index.html` 裡每一個本站 `<script src>` 抓出來逐一驗形態與位置，
  不是列白名單；配「掃到少於 7 支就是尺壞了」的自證、豁免名單長度斷言、以及兩組負控。
  突變 X21（app.js 拿掉 defer）／X22 都實測會紅。
- ⚠️ **既有斷言的尺被撞到時，修的是尺不是實作**：`t13` 原本用整段字串
  `<script src="./js/keyring-unlock.js">` 比對，標籤上多一個 `defer` 就整條失效，
  **而且失效的方向是「安靜地判成找不到」**。已改成「先抓整個標籤、再驗屬性」（同 X15 的教訓）。
  `test/mutate.mjs` 的 K10 目標字串也一起對齊了。

#### ⛔ 這一輪新增的「評估過、刻意不做」

1. **把 `<link rel="manifest">` 與兩個 icon 搬到 `</head>` 前面**：冷啟動 Slow-3G 2225→2216ms，
   **完全在雜訊裡**。它們本來就是低優先權，不值得動。
2. **`defer` ＋ CSS 非阻塞一起做**（不動 splash.js）：2240ms，跟只加 `defer` 一樣，
   卻要多扛「熱啟動 FOUC」的風險。**splash.js 還在 `<head>` 擋著的時候，CSS 怎麼放都沒用。**
3. **CSS 非阻塞 ＋ 把三個 `<link>` 搬到 splash.js 之後**（想讓 splash.js 先下載）：2602ms，**零收益**。

#### 🔺 還沒做、要 PM／老闆拍板的那一塊（−1520ms 就在這裡）

要拿到剩下那 1.5 秒，必須讓**第一次繪製只依賴 `index.html` 本身**，也就是：

1. 把 splash.js 在 `<head>` 就要做完的那一小段（讀 localStorage 外觀快取 → 寫成 `:root` 的
   CSS 變數；熱啟動時掛 `data-splash="off"`）**inline 進 `<head>`**（實測 ~4.2KB，含註解）；
2. `<script src="./js/splash.js">` 移到 `<body>` 最後（**仍然是同步、仍然在 app.js 之前**，
   所以 `window.Splash` 對 app.js 照樣存在）；
3. 三支 CSS 改非阻塞，並補一條熱啟動的 FOUC 守衛
   （`html[data-splash="off"]:not([data-css]) #app{visibility:hidden}` ＋ `onload/onerror` 掛 `data-css`
   ＋ inline 的 2 秒保險絲，**寧可 FOUC 也不可以把 App 永遠藏起來**）。

**成本與風險（要一起講清楚才好拍板）**：
- `js/splash.js` 一個位元組都不用改（`t14 §74` 跟範本比對照樣綠），**但 `t14 §62` 的
  「splash.js 在 `</head>` 之前」會紅** —— 那條是設計契約，改它要有人拍板，不能自己改。
- inline 的那一段會**複製 `splash.js` 的 `onColor()`**（符號字色的算法）。
  這正是本專案第 30／31 條一直在防的「同一條規則活在兩個地方」。
  真要做，**正解是回範本把 `splash.js` 拆成 `splash-boot.js`（小，同步在 head）＋ `splash.js`（其餘）**，
  由範本那條線統一改，各 App 同步 —— 不要在這支 App 自己 inline 一份。
- 熱啟動（Benson 每天從主畫面開、SW 快取熱）**這條路一點好處都沒有**：
  實測 227ms vs 211ms，在雜訊裡。它救的是「Safari 第一次開／快取被清掉／換手機」那一次。

#### 物理下限：熱啟動已經到底了

走 SW 熱啟動、不節流網路，first paint 中位 **211~227ms**，而且**每一個候選都在雜訊內**
（v1.4.1／defer／CSS 非阻塞／inline bootstrap 全部 190~230ms）。
那 200ms 花在「SW 派送 index.html ＋ 解析 HTML ＋ 首次版面」，**網頁層沒有東西可以再拿掉**。
iOS Safari 在導航開始到第一次繪製之間畫布是白的，這件事**沒有網頁層的 API 可以改**
（`theme-color`／`background_color` 只影響系統 UI 與 PWA 的系統啟動圖，不影響 WebView 畫布）。
⇒ 熱啟動那一閃是**物理下限**，要跟 Benson 誠實講；有空間的是冷啟動那條路。

#### 量測工具的新旗標（要重驗照抄）

```bash
node scripts/probe/server.mjs . 8080 &
# 冷啟動（＝Safari 第一次開）：每次取樣前全清，看 SW? 必須是 no
node scripts/probe/bench.mjs --reps=11 --cpu=4 --net=3g --cold=1 --wait=12000 -- 8080:base 8081:候選
# 熱啟動（＝已安裝的 PWA）：先幫每個目標裝好 SW 再量
node scripts/probe/bench.mjs --reps=13 --cpu=4 --net=none --sw=1 --wait=4000 -- 8080:base 8081:候選
```

`--net` 檔位：`none` / `4g`（1.18MB/s、20ms）/ `3g`（89.6KB/s、300ms）/ `slow3g`（50KB/s、400ms）。
⚠️ 一次量超過 4 個目標很容易撞到 10 分鐘的指令逾時，分批跑；**永遠在同一輪之內互相比**。

---

## 待辦 / 已知粗糙處

- **沒有用真金鑰跑過真實 API**（開發環境拿不到、也不該拿）。所有驗證都是用假的 TMDB／OMDb 回應跑的，
  驗的是**行為與錯誤處理**，不是真實資料長相。第一次用真 key 時要重點看：
  TMDB 台灣 `now_playing` 回幾部、`discover` 的台灣訂閱片單品質、OMDb 對台片的失敗率。
- **沒有在真的 iPhone Safari 上跑過**（開發環境沒有瀏覽器可用）。版面 CSS 與定案 demo 幾乎逐條相同，
  但 PWA 加到主畫面、safe-area、iOS 側滑返回這幾件事**要 Benson 實機確認**。
- **⚠️ v1.4.1 的白閃修正在真 iOS 上驗不到**（見第 31 條）。本機真 Chrome 證明的是
  「外部 CSS 全掛時首幀從白色變成深色」與「first paint 沒有變慢」；
  **「他實際看到的白閃有沒有變短／消失」只有 Benson 用他那支 iPhone 能確認。**
  我的判斷是：這一版**不太可能讓白閃完全消失**（白閃主要來自 first paint 之前那 200~400ms，
  對照組一樣有），修掉的是「有可能整片白」的那一類壞情況。要根治得評估
  `apple-touch-startup-image`（見下面「還沒拍板」第 5 條）。
- **`app-template` 的 `index.html` 沒有同步加關鍵路徑 CSS**：這一輪 PM 只授權動 `MIN_SHOW`。
  範本層的坑會複製給每一支新 app，**建議下一輪把第 31 條那塊補進範本**（要 PM 決定）。
- 串流／搜尋列表的平台色塊是背景逐片抓 `watch/providers`（最多 20 次 TMDB 呼叫、24 小時快取）。
  TMDB 沒有每日額度所以可行，但**如果之後覺得慢或吵，第一個該砍的就是這段**（`app.js` 的 `fillProviders`）。
- 片單固定只抓第 1 頁（20 部），沒有做分頁／無限捲動。
- **PTT 爬蟲的 HTML 解析已經在 GitHub Actions 上真跑過一次並成功**（2026-08-23：40 頁 / 797 篇 / 158 篇雷文 /
  比對到 92 篇，10 部片有討論）。開發機仍然連不到 www.ptt.cc，所以**本機測試永遠只驗得到假測資**。
- **比對率第一次是 58%，加了同名破平之後預估 65% 上下**，要下次 Actions 跑完才知道真數字。
  沒比對到的多半是片單以外的片（老片、日韓片、非院線片），那是正常的。
- **鑰匙圈只用「假的模組替身」測過接法**，沒有跑過真的 `keyring-unlock.js`
  （它要 WebCrypto ＋ 真的 `keyring.json`，開發機兩者都沒有）。
  我們對模組行為的四個假設用**對正本檔案的靜態斷言**釘住（t13 §54），模組改版時會紅。
  第一次真的解鎖要看：選人清單出得來嗎、密碼對了金鑰有沒有進來、藥丸有沒有變成「可以編輯」。
- **`xd1104/keyring` 的自動同步 workflow 目前不會更新我們這份**：
  它寫的是各 App 的 `public/` 與 `docs/`，我們沒有那兩個資料夾（模組放在 `js/`）。
  模組改版時要嘛手動複製一次，要嘛請人在那支 workflow 幫我們加一行並指定路徑。
- **PTT 畫面沒有在真的 iPhone Safari 上看過**（開發機沒有瀏覽器）。比例條、44px 重試鈕、
  單行截斷在 375px 上的實際樣子**要 Benson 實機看一眼**。
- 沒有「看過了／不想看」標記，串流片單反覆看到同幾部的機率高。
- **租借／購買沒有價格**（TMDB 不給），只列得到哪裡有。
- 2026-08-23 QA 用 50 個突變壓測過，測試套件目前守得住的清單見 scratchpad 的 `mutverify.mjs`；
  仍有一批「弄壞了測試也不會紅」的地方沒補（例如 SW 攔截 OMDb、`.hide` 樣式失效、
  競態守衛、最近查詢上限、地區/語言常數）。下一輪要動這些地方的人，**先補測試再改**。

## 還沒拍板、要問 Benson 的

1. 列表只有 TMDB 分數（看不到 IMDb／爛番茄），可不可以接受？
   若堅持要，唯一解是「只對前 10 部預抓 OMDb」——額度會吃緊，且捲到後面仍然缺。
2. 綜合分數環的算法目前是**有值分數 ≥2 項才算的純算術平均**（不是 AI 判斷）。要不要加權（例如影評 vs 觀眾）？
3. ~~電影院片單顯示上映日~~ → 2026-08-23 PM 拍板要做，已完成（≤7 天標「本週新上映」）。
4. ~~讓他先勾「我有訂哪些平台」~~ → 2026-08-23 PM 拍板要做，已完成（設定頁，見第 22 條）。
5. **要不要補 `apple-touch-startup-image`？（2026-08-26 提出，未拍板）**
   iOS 15.4 之後會自己用 manifest 的 `background_color` ＋ icon 產系統開場，我們的 manifest
   從 v1.0.0 到現在都是 `#0b0d12`、**一個字都沒改過**，所以系統那張應該本來就是深色的。
   補 startup image 只在兩種情況有差：①他的 iOS 比 15.4 舊；②想讓系統那張長得跟我們的開場一模一樣。
   - **代價**：每一種 iPhone 尺寸／橫直向要各一張 PNG（實務上 10~20 張），要擴充 `scripts/gen-icons.mjs`；
     `sw.js` 的 `SHELL` 要不要收也得決定（收了殼快取會變大）。
   - **風險**：⚠️ **iOS 的系統開場與圖示是「加到主畫面那一刻」抄走的**，
     之後改 manifest／icon 不會生效——**很可能要請 Benson 把 App 從主畫面移除、重新加一次**。
     這件事會讓他的 `localStorage`（片單快取、偏好）看起來像被清掉。
   - **我的建議**：先請他回報「v1.4.1 之後白閃還在不在、系統那張開場是深色還是白色」再決定。
     如果系統那張本來就是深色，補 startup image 的收益接近 0。**這條要 PM／老闆拍板，我沒有自己做。**

---

## 給 AI 的規則

### 開工前
1. 讀這份 CLAUDE.md ＋ `README.md`，特別是上面「已定案、別誤改」。
2. 有份量的任務（新功能、改架構、跨多檔案）→ 建議 Benson 打 `/team-lab` 走 lab-ux → lab-dev → lab-qa。
   小事自己做完就好，開團隊很花額度。

### 動手時
- 繁體中文註解，跟著既有命名與檔案分工（`ui.js` 只產字串、`api.js` 只管資料、`app.js` 才碰 DOM 與狀態）。
- 動 UI 前先看有沒有違反「已定案」那段。
- **改任何行為都要自己實測**，不要只靠讀程式碼推論。

### 收工前的檢查清單
1. `npm test` 全綠。
2. `npm run test:mutate -- --dry` —— **重構過就一定要跑**，確認沒有突變被改到失效。
3. 有動到行為 → 補測試 ＋ 補一條對應的突變，並用 `--only=` 證明它會紅。
4. 有動到使用者看得到的東西 → `HLM_VER` 與 `sw.js` 的 `/* build */` **兩個都 +1**。

### 收工後（強制）
- 新的決策、踩到的雷、新的「別誤改」界線 → **寫回這份 CLAUDE.md，連理由一起寫**。
- 跨專案通用的教訓 → 回報時明講「這條該進手冊」。
- `HLM_VER` 有沒有 +1？

### 溝通
- 結論先給短的；Benson 是老闆、不看技術細節。
- 不確定就說不確定。測不到就說測不到，不要宣稱驗過沒驗的東西。
